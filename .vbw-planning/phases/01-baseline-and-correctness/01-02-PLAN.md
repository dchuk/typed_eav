---
phase: 1
plan: 02
title: Phase 0B - baseline capture, index documentation, status tracker, and Gate 0
type: execute
wave: 2
depends_on: ["01-01"]
cross_phase_deps: []
autonomous: false
effort_override: thorough
skills_used: []
files_modified:
  - "docs/improvement-program.md"
  - "bench/README.md"
  - "bench/database_baseline.rb"
  - "bench/results/phase-0-baseline.json"
forbidden_commands: ["git push", "git tag", "gem push", "scripts/bump-version.sh", "rake release", "gem release", "bundle exec rake release"]
must_haves:
  truths:
    - "The ONLY files created or modified by this plan are docs/improvement-program.md, bench/README.md, bench/database_baseline.rb, and bench/results/phase-0-baseline.json. No production code, spec, factory, migration, Gemfile, gemspec, or .vbw-planning edit."
    - "No push, no tag, no version bump, no publication, no release: `git push`, `git tag`, `gem push`, `scripts/bump-version.sh`, and any `rake release` form are forbidden for the entire plan."
    - "bench/database_baseline.rb is strictly read-only: it issues only SELECT statements against Postgres catalogs/views and read-only size functions. It performs no DDL, no INSERT/UPDATE/DELETE, no ANALYZE, no VACUUM, and no statistics reset."
    - "bench/database_baseline.rb adds no new dependency: it requires only what already ships (active_record / pg via the dummy app's Rails environment). Gemfile, Gemfile.lock, and typed_eav.gemspec are untouched."
    - "The helper captures pg_stat_user_indexes (idx_scan, idx_tup_read, idx_tup_fetch) per index alongside pg_stat_user_tables, so index usage is evidence and not assumption."
    - "Every index on typed_eav_values is documented in docs/improvement-program.md with all seven attributes: exact definition, purpose, expected and type-specific indexed row population, NULL participation, INCLUDE-column use, write-path impact, and the dependent queries that rely on it."
    - "Row accounting is four-way and explicit: physical rows (COUNT(*)), logical live rows (COUNT(*) WHERE field_id IS NOT NULL), orphan rows (COUNT(*) WHERE field_id IS NULL), and a per-typed-column non-null count for each of the eight typed columns."
    - "bytes_per_logical_value is emitted as JSON null with an explicit machine-readable reason string whenever the LOGICAL live row count is zero. The helper never divides by a GREATEST(n, 1) floor and never reports a ratio derived from a substituted denominator."
    - "JSON output separates stable schema metadata (columns, indexes, constraints) from volatile runtime measurements (row counts, sizes, pg_stat rows, pg_settings, timestamps) into distinct top-level sections, with sorted keys throughout."
    - "docs/improvement-program.md is the required status tracker for the whole improvement program: a table whose columns are phase, task, owner, status, dependencies, decision, evidence, commit, and follow-up, seeded with every Phase 1 plan and task."
    - "docs/improvement-program.md records the existing and open performance work carried into this program and the representative generated SQL for the major query shapes (QueryBuilder operator dispatch, FilterQuery single-scope and ALL_SCOPES multimap, include_missing set complement)."
    - "docs/improvement-program.md records the baseline facts with their methodology, including that the 1189 example count came from `bundle exec rspec --dry-run` on Ruby 3.4.4 on 2026-08-15."
    - "GATE 0 is the final task of this plan and is a hard STOP: Phase 1 does not proceed to plan 03 until Sol/Judge has explicitly accepted that (a) all seven bug classes are proved by a failing reproduction or formally rejected in writing, (b) the baseline suite is green except for the intentionally-failing pending regression examples, and (c) the schema and index baseline is captured. Plan 03 must not execute automatically."
    - "Each task completion pauses for Sol review: after each commit, report the verification output and STOP before starting the next task or the next writer."
  artifacts:
    - path: "bench/database_baseline.rb"
      provides: "Read-only Postgres catalog/index/stat/size/row-accounting baseline collector emitting sorted-key JSON"
      contains: "frozen_string_literal: true"
    - path: "bench/results/phase-0-baseline.json"
      provides: "Committed Phase 0 baseline snapshot produced by the helper"
      contains: "bytes_per_logical_value"
    - path: "bench/README.md"
      provides: "How to run the helper, what it does and does not do, the section split, and the empty-database caveat"
      contains: "read-only"
    - path: "docs/improvement-program.md"
      provides: "Baseline facts, per-index documentation for typed_eav_values, program status tracker, open performance work, representative SQL, and the Gate 0 record"
      contains: "Gate 0"
    - path: "docs/improvement-program.md"
      provides: "Program status tracker table"
      contains: "| phase | task | owner | status | dependencies | decision | evidence | commit | follow-up |"
  key_links:
    - from: "bench/README.md"
      to: "bench/database_baseline.rb"
      via: "documents the exact invocation command"
    - from: "docs/improvement-program.md"
      to: "bench/results/phase-0-baseline.json"
      via: "the per-index documentation cites the machine-captured definitions and pg_stat_user_indexes counters"
    - from: "docs/improvement-program.md"
      to: "spec/regressions/improvement_program_phase_0_spec.rb"
      via: "Gate 0 cites each of the seven classes as proved by its reproduction or formally rejected"
---
<objective>
Phase 0B of the mandatory opening contract, serialized after Phase 0A. Capture the Phase 1 baseline: a read-only Postgres baseline helper under `bench/`, its committed JSON result, a `bench/README.md`, and `docs/improvement-program.md` recording versions, schema and index definitions with full per-index documentation, CI configuration, test count, public API surface, major query SQL shapes, open performance work, and the program status tracker.

Then hold GATE 0 - the hard stop that gates every production change in this phase.
</objective>
<context>
@.vbw-planning/phases/01-baseline-and-correctness/01-RESEARCH.md
@.vbw-planning/ROADMAP.md
@.vbw-planning/phases/01-baseline-and-correctness/01-01-PLAN.md

RESEARCH.md is the primary source for the document content: section A (Ruby 3.4.4 local / gemspec floor-ceiling `>= 3.3, < 4.1`; Rails 8.1.3 locally with `.github/compatibility.json` floor/middle/ceiling lanes; PG 17.7 local client, tested 15/16/18; the four CI jobs and the migrate-from-scratch test DB bootstrap; 1189 dry-run examples; RuboCop config), section B (the seven migrations and every index/CHECK-constraint definition), section C (the full public API surface), section D (the QueryBuilder / FilterQuery / resolve_scope SQL shapes), and section F (the exact catalog sources, determinism strategy, and invocation approach for the helper).

RuboCop lints `bench/**` fully: `Style/FrozenStringLiteralComment` applies, `Metrics/MethodLength: 25`, `Metrics/ClassLength: 250`, `Metrics/AbcSize: 25`, `Layout/LineLength: 120`. Build the helper as several small focused methods, not one long script.
</context>
<tasks>
<task type="auto">
  <name>Build the read-only baseline helper, bench README, and committed snapshot</name>
  <files>
    bench/database_baseline.rb
    bench/README.md
    bench/results/phase-0-baseline.json
  </files>
  <action>
`bench/` does not exist yet - create it.

(1) `bench/database_baseline.rb` - a read-only collector. Header comment must state: read-only; no DDL, no data mutation, no ANALYZE/VACUUM/stat reset; no dependency beyond the Rails/ActiveRecord/pg already shipped; and the determinism interpretation (sorted keys and stable structure, NOT byte-identical volatile numbers across DB states). Start with `# frozen_string_literal: true`.

Collect, against the five typed_eav tables (`typed_eav_sections`, `typed_eav_fields`, `typed_eav_options`, `typed_eav_values`, `typed_eav_value_versions`):

STABLE SCHEMA SECTION (`"schema"` - must not vary between runs against the same migrated schema):
  - `pg_indexes.indexdef` - exact index definitions including WHERE / INCLUDE / opclass clauses.
  - `information_schema.columns` - name, data type, nullability, default per column.
  - `pg_constraint` - CHECK constraints (including `chk_te_fields_parent_scope_requires_scope` and `chk_te_sections_parent_scope_requires_scope`) and foreign-key definitions with their ON DELETE actions.

VOLATILE RUNTIME SECTION (`"runtime_snapshot"` - may legitimately differ per run):
  - `pg_settings` - a curated planner-relevant subset only: `work_mem`, `shared_buffers`, `random_page_cost`, `effective_cache_size`, `default_statistics_target`, `max_connections`, `server_version` (`SELECT name, setting, unit FROM pg_settings WHERE name = ANY(...)`). Never `SET` or `ALTER SYSTEM`.
  - `pg_stat_user_tables` - `n_live_tup`, `n_dead_tup`, `seq_scan`, `idx_scan`, `last_vacuum`, `last_analyze`. Read whatever the DB currently reports; do NOT freshen them.
  - `pg_stat_user_indexes` - `indexrelname`, `idx_scan`, `idx_tup_read`, `idx_tup_fetch` for every index on the five tables. This is required, not optional: it is the only evidence of whether an index is used at all.
  - `pg_total_relation_size` / `pg_relation_size` / `pg_indexes_size` - bytes per table and per index.
  - Row accounting for `typed_eav_values`, all four measures reported separately:
      * `physical_rows` = `SELECT COUNT(*) FROM typed_eav_values`
      * `logical_live_rows` = `SELECT COUNT(*) FROM typed_eav_values WHERE field_id IS NOT NULL`
      * `orphan_rows` = `SELECT COUNT(*) FROM typed_eav_values WHERE field_id IS NULL` (the FK is `ON DELETE SET NULL`, so orphans are a real, expected population)
      * `typed_column_non_null_counts` = a per-column `COUNT(col)` for each of the eight typed columns (`string_value`, `text_value`, `boolean_value`, `integer_value`, `decimal_value`, `date_value`, `datetime_value`, `json_value`). Prefer ONE aggregate SELECT emitting all counts rather than eight round trips.
    Plain `COUNT(*)` per table for the other four tables.
  - `bytes_per_logical_value`: when `logical_live_rows > 0`, emit `pg_total_relation_size('typed_eav_values') / logical_live_rows`. When `logical_live_rows == 0`, emit JSON `null` and set a sibling `bytes_per_logical_value_reason` string explaining that no logical values exist so the ratio is undefined. NEVER divide by a substituted denominator - `GREATEST(row_count, 1)` and any equivalent floor are explicitly forbidden, because a `0/1` artifact reads as a real measurement.
  - a generated-at timestamp and the connected server version.

Sort every hash's keys before serialization; write pretty-printed JSON with a trailing newline. Include a top-level `"caveats"` array whose first entry states the empty-database limitation and that these numbers are non-decisional (Phase 2 requires a larger isolated volume/host for representative evidence).

Invocation: target the dummy app's test database, mirroring `spec/spec_helper.rb`'s bootstrap and the CI "Create database and run migrations" step. Document the exact command in the header comment and in the README; whichever form you pick, it must work verbatim as written with `RAILS_ENV=test`. Support writing to a path given as the first ARGV element, defaulting to `bench/results/phase-0-baseline.json`, and print the output path on success. Fail loudly with a clear message if the catalog reads error or the connection is unavailable - do not silently emit a partial document.

(2) `bench/README.md` - what the helper does, the exact command to run it, what the two JSON sections mean and why they are separated, the explicit read-only / no-mutation guarantee, the four-way row accounting and what each measure means, the `bytes_per_logical_value` null-with-reason contract, the empty-database caveat, and a note that results are regenerated (not hand-edited).

(3) `bench/results/phase-0-baseline.json` - generated by actually RUNNING the helper (never hand-written). If the dummy test database is not present or not migrated, bring it up the same way `spec/spec_helper.rb` / CI do (creating and migrating the TEST database is expected setup, not a violation of the helper's read-only contract, which governs the helper itself).

STOP CONDITIONS (report and halt): catalog access failure; missing exact definitions, settings, counts, or caveats; any new dependency; any mutation of database state by the helper; any edit outside the three allowed files for this task; a dirty worktree at start.
  </action>
  <verify>
Confirm a clean worktree BEFORE starting: `git status --porcelain` (must be empty).

1. Helper executes and produces output: run the documented command through `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec ...` with `RAILS_ENV=test`; exit status 0; `bench/results/phase-0-baseline.json` is written.
2. Output keys present: the JSON parses and contains the `schema`, `runtime_snapshot`, and `caveats` top-level keys; `schema` contains index definitions for all five typed_eav tables and both `chk_te_*_parent_scope_requires_scope` CHECK constraints; `runtime_snapshot` contains `pg_stat_user_indexes` entries, `pg_settings`, sizes, all four row-accounting measures, and the eight per-typed-column non-null counts.
3. Divide-by-one guard: `grep -nE "GREATEST\(|, *1\)|\|\| *1" bench/database_baseline.rb` shows no denominator floor; on the current (empty) database the JSON's `bytes_per_logical_value` is `null` and `bytes_per_logical_value_reason` is a non-empty string.
4. Section separation: run the helper twice to two scratch paths and confirm the `schema` section is byte-identical between runs while `runtime_snapshot` is allowed to differ.
5. Read-only proof: `grep -inE "insert |update |delete |alter |create |drop |vacuum|analyze|pg_stat_reset" bench/database_baseline.rb` returns no SQL-executing hit (documentation strings such as "no ANALYZE/VACUUM" are the only acceptable matches).
6. No new dependency: `git status --porcelain` shows no change to `Gemfile`, `Gemfile.lock`, or `typed_eav.gemspec`.
7. Lint: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop bench` - no offenses.
8. Full suite unaffected: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures, pending count unchanged from plan 01.
9. Whitespace: `git diff --check` - no output. Scope guard: `git status --porcelain` lists only the three allowed paths.
10. Release guard: `git tag --points-at HEAD` is empty; no push was performed.
  </verify>
  <done>
The three bench files exist, the JSON was produced by running the helper (not hand-written), all ten checks pass, and exactly one commit was made: `feat(bench): add read-only database baseline helper and phase 0 snapshot`.

Then STOP and report for Sol review before starting the next task.
  </done>
</task>
<task type="auto">
  <name>Write docs/improvement-program.md - baseline, per-index documentation, and status tracker</name>
  <files>
    docs/improvement-program.md
  </files>
  <action>
Create `docs/improvement-program.md` as the program's single required status document. Sections, in order:

(1) BASELINE FACTS, sourced from RESEARCH.md sections A-D with the methodology stated for each fact:
  - Ruby: local 3.4.4 (`/Users/darrindemchuk/.rbenv/versions/3.4.4/bin`), gemspec `>= 3.3, < 4.1`, CI series 3.3 / 3.4 / 4.0, tooling pinned to 3.4.
  - Rails: 8.1.3 resolved locally, gemspec `>= 7.2, < 8.2`, CI lanes floor `~> 7.2.0` / middle `~> 8.0.0` / ceiling `~> 8.1.0`.
  - PostgreSQL: local client 17.7, `.github/compatibility.json` min 15 / max 18 / tested 15, 16, 18; pg gem 1.6.3. Note the ACTUAL connected server version is captured in the baseline JSON's `pg_settings.server_version`.
  - CI: the four jobs (compatibility, lint, test matrix, consumer-install), `.github/compatibility.json` as the single source of truth for the matrix, and the migrate-from-scratch test DB bootstrap.
  - Test count: "1189 examples" with the explicit methodology line - `bundle exec rspec --dry-run --format progress`, Ruby 3.4.4, 2026-08-15 - and the note that a dry-run count confirms collection, not pass/fail.
  - Public API surface: RESEARCH.md section C in full.

(2) SCHEMA AND INDEX BASELINE. List the seven migrations and their tables/columns/constraints, pointing at `bench/results/phase-0-baseline.json` as the machine-captured source of truth. Then, for EVERY index on `typed_eav_values`, write a subsection documenting all seven attributes:
  - exact definition (copy the `indexdef` verbatim from the JSON);
  - purpose - the access pattern it exists to serve;
  - expected indexed rows and how that population is type-specific (a `(field_id, integer_value)` index only carries rows whose field is an integer-typed field; every other field type's rows sit in the index with a NULL payload column or are excluded by a partial predicate - state which, per index);
  - NULL participation - whether NULLs are indexed, and what that means for `:is_null` / `:is_not_null` and for the `include_missing` set-complement path;
  - INCLUDE-column use - which columns are INCLUDE'd, why (index-only scans returning `entity_id` / `entity_type` without a heap fetch), and the size cost;
  - write-path impact - what each INSERT/UPDATE of a value row costs across this index;
  - dependent queries - the concrete query shapes from RESEARCH.md section D that rely on this index, named explicitly.
  Cover the unique `(entity_type, entity_id, field_id)` index, all six `(field_id, <typed>_value) INCLUDE (entity_id, entity_type)` covering indexes (calling out `string_value`'s `text_pattern_ops` opclass and what it does and does not accelerate), and the partial GIN index on `json_value WHERE json_value IS NOT NULL`. Cite each index's `pg_stat_user_indexes` counters from the JSON and state plainly that on the current empty/near-empty database those counters are non-decisional.

(3) MAJOR QUERY SHAPES AND REPRESENTATIVE GENERATED SQL. RESEARCH.md section D (QueryBuilder operator dispatch, FilterQuery single-scope vs ALL_SCOPES multimap, include_missing set complement, resolve_scope chokepoint), each accompanied by the actual generated SQL captured with `.to_sql` from a scratch console or a throwaway script - do not hand-write SQL from memory. Include at minimum: a scalar `:eq`, a `:gt`, a `:between`, a `:contains` (showing the LIKE escaping), a jsonb `:any_eq`, a two-filter AND, and an `include_missing: true` + `:is_null` complement.

(4) OPEN AND EXISTING PERFORMANCE WORK. Enumerate the performance work already carried into this program with its current state: the deferred REQ-05 read-path/materialized-index idea (out of milestone scope, revisit candidate), Phase 2's scalar/partial-index and text-search benchmarking, Phase 3's BulkRead/BulkWrite/backfill profiling, Phase 4's durability comparison, and Phase 5's storage tournament. For each, record what evidence exists today (for most: none) and what evidence would be required to decide.

(5) STATUS TRACKER - the required, load-bearing table. Header exactly:
`| phase | task | owner | status | dependencies | decision | evidence | commit | follow-up |`
Seed one row per Phase 1 plan AND per task within it (plans 01 through 06, including Gate 0 and Gate 1), with dependencies expressed as the serialized wave chain, owner set per the ROADMAP roles (Sol High for architecture/review/gates, Luna Max for bounded implementation), status set to the truthful current state, and commit/evidence filled where already known. Every later plan appends to this table rather than creating a parallel record.

(6) SEVEN CORRECTNESS CLASSES. One row/entry per class: number, short title, mechanism summary, the reproduction that proves it in `spec/regressions/improvement_program_phase_0_spec.rb`, and its current state (proved / formally rejected). Leave a clearly-marked placeholder heading for Gate 1 results, which plan 06 fills in.

Write with the same tone and structure conventions as the existing `docs/adr/*.md` files. Do not edit any other file.

STOP CONDITIONS: any index on `typed_eav_values` documented with fewer than the seven required attributes; any SQL sample written from memory rather than captured; any edit outside `docs/improvement-program.md`.
  </action>
  <verify>
1. `grep -q "1189" docs/improvement-program.md` succeeds and the surrounding text names `--dry-run` as the methodology.
2. Status tracker present with the exact required header: `grep -qF "| phase | task | owner | status | dependencies | decision | evidence | commit | follow-up |" docs/improvement-program.md`.
3. Per-index completeness: for every `indexdef` on `typed_eav_values` in `bench/results/phase-0-baseline.json`, the index name appears in `docs/improvement-program.md` and its subsection contains all seven attribute labels (definition, purpose, indexed rows, NULL participation, INCLUDE, write impact, dependent queries). Verify by listing the index names from the JSON and grepping each.
4. Representative SQL is captured, not invented: each SQL block in section 3 was produced by `.to_sql` and the summary records how it was captured.
5. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop docs` - no offenses (or no Ruby files to inspect).
6. `git status --porcelain` lists only `docs/improvement-program.md`; `git diff --check` produces no output.
7. Release guard: `git tag --points-at HEAD` is empty; no push was performed.
  </verify>
  <done>
`docs/improvement-program.md` exists with all six sections, every `typed_eav_values` index documented against the seven attributes, captured representative SQL, the open-performance-work register, and the seeded status tracker. All seven checks pass. Exactly one commit: `docs(improvement-program): capture phase 0 baseline index documentation and status tracker`.

Then STOP and report for Sol review before starting the next task.
  </done>
</task>
<task type="auto">
  <name>GATE 0 - assemble the acceptance package and STOP for Sol/Judge sign-off</name>
  <files>
    docs/improvement-program.md
  </files>
  <action>
GATE 0 is a hard stop. No production code in this phase may change before it is accepted. Assemble the acceptance package, record it, and halt.

Gather the evidence (all read-only, on Ruby 3.4.4):
  a. BUG CLASSES PROVED OR FORMALLY REJECTED. For each of the seven classes, state either the reproduction that fails today (naming the example and quoting its failure output) or - if the class turned out to be already-correct - a written formal rejection giving the evidence that the behavior is correct and the reason the class is withdrawn. There is no third state: a class that is neither proved nor formally rejected blocks the gate.
  b. BASELINE SUITE GREEN EXCEPT INTENTIONAL FAILURES. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec --format progress` - record the exact example / failure / pending counts. Zero failures. The only pending examples attributable to this phase are the intentionally-failing regression reproductions from plan 01; enumerate them by name so the gate reviewer can confirm the count matches. Any other failing or pending example is a blocker, not a footnote.
  c. SCHEMA AND INDEX BASELINE CAPTURED. `bench/results/phase-0-baseline.json` is committed, its `schema` section reproduces byte-identically on re-run, and every `typed_eav_values` index is documented against the seven attributes in `docs/improvement-program.md`.

Record all three under a `## Gate 0` heading in `docs/improvement-program.md`, with an explicit `Status: awaiting Sol/Judge acceptance` line, the exact commands and outputs used as evidence, and a statement that plan 03 is blocked until acceptance is recorded.

Then STOP. Do NOT start plan 03. Do NOT begin any production change. Report to the orchestrator that Gate 0 is assembled and awaiting explicit Sol/Judge acceptance, and hand over the three-part evidence package. Execution of plan 03 is manual-release-only: it must not be scheduled automatically off the back of this plan's completion.

STOP CONDITIONS (blockers, report rather than work around): any bug class neither proved nor formally rejected; any suite failure; any pending example not attributable to plan 01's reproductions; the `schema` section not reproducing identically; any index missing a documented attribute.
  </action>
  <verify>
1. `grep -q "^## Gate 0" docs/improvement-program.md` succeeds and the section contains all three evidence parts plus the `Status: awaiting Sol/Judge acceptance` line.
2. All seven classes appear under Gate 0 with an explicit proved-or-rejected verdict; no class is left unmarked.
3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures, and the pending count equals the number of plan-01 reproductions.
4. Re-running the baseline helper to a scratch path yields a `schema` section byte-identical to the committed JSON's.
5. `git status --porcelain` lists only `docs/improvement-program.md`; `git diff --check` produces no output.
6. No production file changed anywhere in this plan: `git diff --stat <phase-start-commit>..HEAD -- app lib db` is empty.
7. Release guard: `git tag --points-at HEAD` is empty; `git diff --stat -- lib/typed_eav/version.rb CHANGELOG.md` is empty; no push was performed.
  </verify>
  <done>
The Gate 0 section records the three-part evidence package and marks itself awaiting acceptance. Exactly one commit: `docs(improvement-program): record gate 0 evidence and request acceptance`.

Then STOP and hand the package to Sol/Judge. Plan 03 stays blocked until acceptance is explicitly recorded.
  </done>
</task>
</tasks>
<verification>
1. `bench/database_baseline.rb`, `bench/README.md`, `bench/results/phase-0-baseline.json`, and `docs/improvement-program.md` all exist.
2. The documented invocation command runs to exit status 0 on Ruby 3.4.4 and regenerates the JSON.
3. `ruby -rjson -e 'j=JSON.parse(File.read("bench/results/phase-0-baseline.json")); abort unless %w[schema runtime_snapshot caveats].all? { |k| j.key?(k) }'` succeeds.
4. The JSON contains `pg_stat_user_indexes` data, all four row-accounting measures, the eight per-typed-column non-null counts, and a `bytes_per_logical_value` that is `null` with a reason on the empty database.
5. `grep -qF "| phase | task | owner | status | dependencies | decision | evidence | commit | follow-up |" docs/improvement-program.md` succeeds.
6. `grep -q "^## Gate 0" docs/improvement-program.md` succeeds and the section names all seven classes with a proved-or-rejected verdict.
7. `git status --porcelain` shows no change to Gemfile, Gemfile.lock, or typed_eav.gemspec; `git diff --stat <phase-start-commit>..HEAD -- app lib db` is empty.
8. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop bench docs` reports no offenses; `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` reports 0 failures; `git diff --check` produces no output.
9. `git tag --points-at HEAD` is empty and no push occurred.
</verification>
<success_criteria>
- Baseline is captured and committed: versions, schema and index definitions with full per-index documentation, CI configuration, test count, public API surface, major query SQL shapes, and reproducible benchmark scaffolding.
- The `bench/` helper is read-only, dependency-free beyond current Rails/ActiveRecord/pg, captures pg_stat_user_indexes and four-way row accounting, and reports `bytes_per_logical_value` as null-with-reason rather than dividing by a substituted denominator.
- `docs/improvement-program.md` is the program's status tracker with the required nine columns, plus open performance work and captured representative SQL.
- Gate 0 is assembled and STOPPED for Sol/Judge acceptance; no production code changed and plan 03 remains blocked.
- No push, tag, version bump, publication, or release occurred.
</success_criteria>
<output>
02-SUMMARY.md
</output>
</output>

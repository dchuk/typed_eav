---
phase: 1
plan: 03
title: Value lifecycle repairs - pending false, invalid-cast persistence, version-group leak
type: execute
wave: 3
depends_on: ["01-02"]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: [tdd]
files_modified:
  - "app/models/typed_eav/value.rb"
  - "lib/typed_eav/versioning/subscriber.rb"
  - "spec/regressions/improvement_program_phase_0_spec.rb"
forbidden_commands: ["git push", "gem push", "scripts/bump-version.sh", "rake release", "git tag"]
must_haves:
  truths:
    - "Correctness class 1 is repaired: a Value whose pending value is the literal boolean `false` applies that value once the field is resolved, instead of silently dropping it."
    - "Correctness class 2 is repaired: an invalid cast is rejected on EVERY subsequent validation of the same in-memory object, not only the first; the record cannot be saved after a consumed first check."
    - "Correctness class 4 is repaired: `pending_version_group_id` is one-shot - after a version row consumes it, a later standalone save of the same in-memory Value object no longer inherits the stale bulk UUID."
    - "The pair-shaped duplicate-instance handling in BulkWrite.execute / execute_pairs is untouched and its existing specs still pass - class 4's repair is narrowly the never-cleared ivar."
    - "Each of the three defects is one task, one coherent concern, one conventional atomic commit, and each commit removes the `pending(...)` marker for ONLY its own reproduction in spec/regressions/improvement_program_phase_0_spec.rb."
    - "Public API is unchanged: no method removed or renamed on TypedEAV::Value, no signature change, no new required argument."
  artifacts:
    - path: "app/models/typed_eav/value.rb"
      provides: "Repaired apply_pending_value guard, non-self-consuming cast-invalid validation, and one-shot version-group correlation"
      contains: "apply_pending_value"
    - path: "spec/regressions/improvement_program_phase_0_spec.rb"
      provides: "Reproductions 1, 2 and 4 now passing (pending markers removed)"
      contains: "IMPROVEMENT PROGRAM 4"
  key_links:
    - from: "app/models/typed_eav/value.rb"
      to: "lib/typed_eav/versioning/subscriber.rb"
      via: "the one-shot version-group correlation contract shared between the ivar owner and its only reader"
---
<objective>
Repair the three correctness classes that live in `app/models/typed_eav/value.rb` - the serialized-writer zone the ROADMAP names explicitly. Three tasks, strictly in order, one atomic commit each, each flipping exactly one Phase 0A reproduction from pending to passing.

This plan owns `value.rb` for the whole of wave 3; no other Phase 1 plan touches it.
</objective>
<context>
@.vbw-planning/phases/01-baseline-and-correctness/01-RESEARCH.md
@.vbw-planning/phases/01-baseline-and-correctness/01-01-PLAN.md
@/Users/darrindemchuk/.claude/skills/tdd/SKILL.md

RESEARCH.md section E classes 1, 2 and 4 give the mechanism and exact line references; section G's "Net overlap map" is why these three are one serialized plan. The reproductions written in plan 01 are the red tests - do not rewrite their assertions to fit an implementation; make the implementation satisfy them.

Relevant existing behavior that must survive:
- `spec/models/typed_eav/value_spec.rb:765-785` ("late-field-assignment caveat") locks the `UNSET_VALUE` sentinel path through `apply_pending_value`.
- `lib/typed_eav/bulk_write.rb` `execute` / `execute_pairs` pair-shaped duplicate-instance handling is deliberate and already correct.
- `spec/lib/typed_eav/bulk_write_per_record_spec.rb`, `spec/lib/typed_eav/bulk_operations_spec.rb`, and `spec/models/typed_eav/value_versioning_integration_spec.rb` lock the current versioning correlation behavior.
</context>
<tasks>
<task type="auto">
  <name>Repair class 1: pending false assignment order</name>
  <files>
    app/models/typed_eav/value.rb
    spec/regressions/improvement_program_phase_0_spec.rb
  </files>
  <action>
`apply_pending_value` (`app/models/typed_eav/value.rb:354-369`) guards with `return unless @pending_value && field`. A literal `false` pending value is indistinguishable from "nothing pending" under that truthiness test, so the assignment is silently dropped and `@pending_value` is never cleared.

Fix the guard so "has a pending value" is tested by presence, not truthiness - e.g. track pendingness explicitly (a dedicated `@pending_value_set` boolean, or a distinct NOT_PENDING sentinel in the same style as the existing `UNSET_VALUE` constant) rather than relying on `@pending_value` being truthy. Whichever mechanism you choose, keep it consistent with the file's existing sentinel idiom and make sure:
  - the `UNSET_VALUE` sentinel branch still dispatches to `apply_field_default` exactly as today;
  - `@pending_value` (and any new pendingness flag) is cleared after application, so a second `apply_pending_value` call is a no-op;
  - `nil` as a deliberately-pending value behaves consistently with the new presence test - document the chosen semantics in a comment;
  - the `value=` `else` branch at `value.rb:119-122` sets the pendingness state in step with the new guard.

Then remove the `pending(...)` line from the "IMPROVEMENT PROGRAM 1" reproduction only - leave the other six pending markers untouched. Same commit.
  </action>
  <verify>
1. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb --format documentation` - reproduction 1 PASSES; reproductions 2-7 still report as pending (failing as expected); 0 failures.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/models/typed_eav/value_spec.rb` - 0 failures (the UNSET_VALUE late-field-assignment caveat still holds).
3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures.
4. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop app/models/typed_eav/value.rb spec/regressions/improvement_program_phase_0_spec.rb` - no offenses.
5. `git diff --check` - no output.
  </verify>
  <done>
Reproduction 1 passes, the sentinel path is intact, all five checks pass, and one commit exists: `fix(value): apply pending false values once the field resolves`.
  </done>
</task>
<task type="auto">
  <name>Repair class 2: invalid-cast persistence across repeated validation</name>
  <files>
    app/models/typed_eav/value.rb
    spec/regressions/improvement_program_phase_0_spec.rb
  </files>
  <action>
`validate_value` (`app/models/typed_eav/value.rb:388-395`) self-consumes the invalid-cast flag: it adds `errors.add(:value, :invalid)` and then resets `@cast_was_invalid = false`, so any second `valid?` / `invalid?` / `save` on the same in-memory object passes and can persist a nil/partial row.

Make the flag sticky instead of one-shot: `@cast_was_invalid` must remain true until the value is REASSIGNED (i.e. until `value=` runs again and produces a fresh cast result at `value.rb:116-118`), not until it is read. Remove the reset from `validate_value`; the flag's lifecycle belongs entirely to the write path.

Constraints:
  - a subsequent VALID assignment must clear the flag and allow the record to validate and save normally - assert this explicitly so the fix does not permanently poison the object;
  - `errors.add(:value, :invalid)` and the early `return` semantics (skipping the required / typed-value checks when the cast failed) stay as they are;
  - no new public method or attribute on `TypedEAV::Value`.

Then remove the `pending(...)` line from the "IMPROVEMENT PROGRAM 2" reproduction only. Same commit.
  </action>
  <verify>
1. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb --format documentation` - reproductions 1 and 2 PASS; 3-7 still pending; 0 failures.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/models/typed_eav/value_spec.rb spec/regressions/known_bugs_spec.rb` - 0 failures.
3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures.
4. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop app/models/typed_eav/value.rb spec/regressions/improvement_program_phase_0_spec.rb` - no offenses.
5. `git diff --check` - no output.
  </verify>
  <done>
Reproduction 2 passes, a valid reassignment still clears the flag, all five checks pass, and one commit exists: `fix(value): keep invalid-cast rejection sticky across repeated validation`.
  </done>
</task>
<task type="auto">
  <name>Repair class 4: one-shot version-group correlation under object reuse</name>
  <files>
    app/models/typed_eav/value.rb
    lib/typed_eav/versioning/subscriber.rb
    spec/regressions/improvement_program_phase_0_spec.rb
  </files>
  <action>
`pending_version_group_id` (`app/models/typed_eav/value.rb:153`) is stamped by `lib/typed_eav/bulk_write.rb:261-273` and never cleared, and `lib/typed_eav/versioning/subscriber.rb:145` reads it preferentially (`value.pending_version_group_id || context[:version_group_id]`). A Value object reused for a later standalone save therefore inherits the stale bulk UUID forever.

Make the correlation genuinely one-shot: after the version row consumes the ivar, clear it, so the next save falls through to `context[:version_group_id]` (or NULL when there is no context). Prefer consuming it at the single read site in `Subscriber#write_version_row` - read into a local, null the ivar, then build the row from the local - so the ivar's contract is "consumed on read". If a small consuming accessor on `Value` reads better, add it there and call it from the subscriber; either way keep the change narrow and update the long explanatory comment at `value.rb:130-152` to document the consume-on-read lifecycle.

Hard constraints:
  - do NOT touch `BulkWrite.execute` / `execute_pairs` pair-shaped duplicate-instance handling - it is deliberate and already correct;
  - a bulk write that stamps several Values must still correlate ALL of their version rows to the same UUID within that one bulk call;
  - `attr_accessor :pending_version_group_id` stays public API (it is documented in the baseline's public API surface) - do not remove or rename it;
  - clearing must be safe when versioning is disabled or no version row is written (the ivar simply goes unused).

Then remove the `pending(...)` line from the "IMPROVEMENT PROGRAM 4" reproduction only. Same commit.
  </action>
  <verify>
1. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb --format documentation` - reproductions 1, 2 and 4 PASS; 3, 5, 6, 7 still pending; 0 failures.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/lib/typed_eav/bulk_write_per_record_spec.rb spec/lib/typed_eav/bulk_operations_spec.rb spec/models/typed_eav/value_versioning_integration_spec.rb` - 0 failures (bulk correlation and pair-shaped handling intact).
3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures.
4. `git diff --stat` shows no change to `lib/typed_eav/bulk_write.rb`.
5. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop app/models/typed_eav/value.rb lib/typed_eav/versioning/subscriber.rb spec/regressions/improvement_program_phase_0_spec.rb` - no offenses.
6. `git diff --check` - no output.
  </verify>
  <done>
Reproduction 4 passes, within-bulk correlation still works, `bulk_write.rb` is untouched, all six checks pass, and one commit exists: `fix(versioning): consume pending version group id so reused values do not inherit stale uuids`.
  </done>
</task>
</tasks>
<verification>
1. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb` - reproductions 1, 2 and 4 pass; 3, 5, 6, 7 remain pending; 0 failures.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures.
3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop` - no offenses.
4. `git diff --check` produces no output.
5. `git log --oneline -3` shows exactly three commits, one per defect, in conventional format.
6. `git diff --stat HEAD~3..HEAD` touches only `app/models/typed_eav/value.rb`, `lib/typed_eav/versioning/subscriber.rb`, and `spec/regressions/improvement_program_phase_0_spec.rb`.
7. Public API check: `TypedEAV::Value` still responds to `value`, `value=`, `value_column`, `history`, `revert_to`, and `pending_version_group_id`.
</verification>
<success_criteria>
- Correctness classes 1, 2 and 4 are each repaired in their own bounded task with one conventional atomic commit, and their pending reproductions flip to passing.
- No writer overlap: this plan is the sole owner of `app/models/typed_eav/value.rb` in Phase 1's repair sequence.
- Validation, auditability, and public API contracts are unchanged; the full suite and RuboCop stay green on Ruby 3.4.4.
</success_criteria>
<output>
03-SUMMARY.md
</output>

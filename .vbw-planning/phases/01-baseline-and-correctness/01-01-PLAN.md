---
phase: 1
plan: 01
title: Phase 0A - pending reproductions for seven correctness classes
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: false
effort_override: thorough
skills_used: [tdd]
files_modified: ["spec/regressions/improvement_program_phase_0_spec.rb"]
forbidden_commands: ["git push", "git tag", "gem push", "scripts/bump-version.sh", "rake release", "gem release", "bundle exec rake release"]
must_haves:
  truths:
    - "The ONLY file created or modified by this plan is spec/regressions/improvement_program_phase_0_spec.rb. Zero edits to production code (app/, lib/), factories (spec/factories/), spec/spec_helper.rb, spec/rails_helper.rb, db/migrate/, docs/, bench/, or .vbw-planning/."
    - "No push, no tag, no version bump, no publication, no release: `git push`, `git tag`, `gem push`, `scripts/bump-version.sh`, and any `rake release` form are forbidden for the entire plan."
    - "All seven reproductions are marked with RSpec's in-example `pending(\"...\")` (NOT `xit`/`skip`), so RSpec asserts each one currently FAILS; if any reproduction passes, RSpec fails the run and Dev must STOP and report rather than weaken the assertion."
    - "Class 1 is reproduced behaviorally through the PUBLIC constructor in BOTH hash orders - `TypedEAV::Value.new(field: f, value: false)` AND `TypedEAV::Value.new(value: false, field: f)` - not only by poking @pending_value."
    - "Class 1 reproductions distinguish five distinct pending states as separate examples: (a) no pending value at all, (b) the UNSET_VALUE sentinel, (c) an explicit nil, (d) the literal boolean false, (e) an ordinary truthy value. Each asserts its own documented outcome; the false case must NOT be conflated with (a) or (c)."
    - "Class 2 is reproduced observationally: `valid?` then `valid?` again then `save` on the SAME in-memory object must all reject, and a separate example asserts that correction by REASSIGNMENT to a valid value clears the rejection and permits save."
    - "Class 3 is reproduced for BOTH Field and Section across the three exact partition fixtures under one entity_type: fallback/global (scope nil, parent_scope nil), scope-only (scope set, parent_scope nil), and full tuple (scope and parent_scope both set). Reordering inside one fixture must leave the other two fixtures' sort_order values untouched."
    - "Class 4 is reproduced by REUSING the same in-memory Value object for a later standalone save after a bulk write, asserting the second version row carries no stale bulk UUID; the assertion never touches BulkWrite pair-shaped duplicate-instance handling, which is deliberate and already correct."
    - "Class 5 is reproduced with the Integer \"3.7\" case: the write side rejects it (`field.cast(\"3.7\") == [nil, true]`) and the query side is asserted to agree rather than silently truncating or matching."
    - "Class 6 is reproduced as strict `:between` shape: a Range and an exactly-two-element Array are accepted, and 0-, 1-, and 3-or-more-element Arrays are asserted to raise ArgumentError naming :between."
    - "Class 7 is reproduced across the full default-value family matrix, one example per family: numeric min/max, text length/pattern, option inclusion (single and multi), array size/content, Currency completeness and allowed currency, and Percentage bounds."
    - "Every reproduction uses only factories that already exist - no new or modified factory definitions. If a family in the class-7 matrix has no existing factory, build the field inline from the existing base factory with `options:` overrides rather than adding a factory."
    - "Reproductions use entity_type fixtures distinct from existing spec fixtures (never OrdConcur, OrdPart, or any entity_type already used by spec/models/typed_eav/field_spec.rb) so they cannot interact with the pre-existing :concurrency context."
    - "Full RSpec run shows no NEW failures beyond the expected pending-as-failing reproductions; the 1189 pre-existing examples remain unaffected."
    - "Task completion pauses for Sol review: after the commit, report the verification output and STOP. Do not start plan 02 or any other writer until Sol has reviewed and released this plan."
  artifacts:
    - path: "spec/regressions/improvement_program_phase_0_spec.rb"
      provides: "Pending behavioral reproductions, one describe block per confirmed correctness class"
      contains: "IMPROVEMENT PROGRAM 1"
    - path: "spec/regressions/improvement_program_phase_0_spec.rb"
      provides: "Reproduction for the seventh correctness class across the default-value family matrix"
      contains: "IMPROVEMENT PROGRAM 7"
  key_links:
    - from: "spec/regressions/improvement_program_phase_0_spec.rb"
      to: "app/models/typed_eav/value.rb"
      via: "reproductions 1, 2 and 4 exercise apply_pending_value / validate_value / pending_version_group_id"
    - from: "spec/regressions/improvement_program_phase_0_spec.rb"
      to: "app/models/typed_eav/section.rb"
      via: "reproduction 3 exercises Section reorder across the three exact partition fixtures"
    - from: "spec/regressions/improvement_program_phase_0_spec.rb"
      to: "lib/typed_eav/query_builder.rb"
      via: "reproductions 5 and 6 exercise comparison-operator casting agreement and :between shape validation"
---
<objective>
Phase 0A of the mandatory opening contract. Create ONE new spec file containing behavioral reproductions for all seven confirmed correctness classes - each marked `pending` so it reports as failing-as-expected today and will flip to passing when the matching repair lands in plans 03-05. No production fix, no factory change, no helper change.

This is deliberately a single-task plan: the ROADMAP binds Phase 1 to open with exactly two serialized tasks (Phase 0A then Phase 0B), one coherent concern and one conventional atomic commit each.
</objective>
<context>
@.vbw-planning/phases/01-baseline-and-correctness/01-RESEARCH.md
@.vbw-planning/ROADMAP.md
@/Users/darrindemchuk/.claude/skills/tdd/SKILL.md

Research section E gives the exact mechanism, file:line, existing coverage gap, and a validated reproduction shape for each of the seven classes - follow those shapes and then EXTEND them to the behavioral matrix this plan's must_haves require. Section "Relevant Patterns" documents the existing `spec/regressions/*` naming convention (`describe "ANALYSIS N.N: ..."` / `"REVIEW: ..."`) that the new file should extend as `"IMPROVEMENT PROGRAM N: ..."`. `.rubocop.yml` already excludes `spec/regressions/**/*` from `RSpec/DescribeClass` and `spec/**/*` from `Metrics/BlockLength`, so string-literal describes and long nesting are lint-clean.

Existing lineage file to mirror for style: `spec/regressions/known_bugs_spec.rb`.
</context>
<tasks>
<task type="auto">
  <name>Add pending reproductions for all seven correctness classes</name>
  <files>
    spec/regressions/improvement_program_phase_0_spec.rb
  </files>
  <action>
Create the file (it does not exist yet) with `# frozen_string_literal: true`, the same `require` header as `spec/regressions/known_bugs_spec.rb` (read it first and copy it exactly), and seven top-level `RSpec.describe "IMPROVEMENT PROGRAM N: <short title>" do` blocks in class order 1..7.

Inside each example, call RSpec's `pending("<one-line statement of the defect>")` as the FIRST statement of the example body, then write the assertion for the CORRECT (desired) behavior. This is load-bearing: RSpec fails the run if a `pending` example unexpectedly passes, which is exactly the "already-correct behavior forced pending" stop condition. Do NOT use `xit`, `skip`, or `it ..., pending: true` - those do not execute the body and cannot detect an already-correct behavior.

Examples that assert behavior which is ALREADY correct today (the guard-rail assertions called out below - e.g. UNSET_VALUE dispatch, ordinary-value assignment, well-formed `:between` input) must NOT be marked pending; they are affirmative locks that must pass now and keep passing after the repairs. Keep them in the same describe block, clearly named, so the repair plans cannot over-tighten.

REPRODUCTION MATRIX - write every case below.

1. "IMPROVEMENT PROGRAM 1: pending false assignment order" (`app/models/typed_eav/value.rb:354-369`, guard `return unless @pending_value && field`; `value=` else-branch at :118-123). Use `:boolean_field` and `:contact`. Write these examples:
   - PENDING: `TypedEAV::Value.new(value: false, field: boolean_field, entity: contact)` - value: listed BEFORE field: so `value=` runs while `field` is still unresolved - then assert `v.value` is `false` and, after `v.save!`, the persisted `boolean_value` is `false` (not nil).
   - PENDING: `TypedEAV::Value.new(field: boolean_field, value: false, entity: contact)` - the other hash order. Assert the same outcome. Both orders are part of the public contract; do not assume AR applies them in source order.
   - PENDING: deferred field resolution - `v = TypedEAV::Value.new(entity: contact); v.value = false; v.field = boolean_field` then drive the same public path the existing "late-field-assignment caveat" spec uses (`spec/models/typed_eav/value_spec.rb:765-785` - read it and mirror its mechanism) and assert `v.value == false`.
   - AFFIRMATIVE (not pending): no pending value at all - `TypedEAV::Value.new(entity: contact, field: boolean_field)` leaves `value` nil and resolving the field is a no-op.
   - AFFIRMATIVE (not pending): the `UNSET_VALUE` sentinel path still dispatches to the field default exactly as today (this is the behavior `value_spec.rb:765-785` locks).
   - PENDING: explicit `nil` as a deliberately-pending value - assert the documented outcome (nil applies as nil, distinct from "nothing was pending"). State the expected semantics in the example name so plan 03's repair has an unambiguous target.
   - AFFIRMATIVE (not pending): an ordinary truthy value (e.g. `true` on the boolean field, or `"hello"` on a `:text_field`) assigned before field resolution still applies.
   These five states - none / UNSET_VALUE / nil / false / ordinary - must be five separately named examples so the repair cannot collapse them.

2. "IMPROVEMENT PROGRAM 2: invalid-cast persistence" (`validate_value`, `value.rb:388-412`, self-consumes `@cast_was_invalid`). Use `:integer_field` and `:contact`. Write:
   - PENDING (observational repeated validation): `v = TypedEAV::Value.new(entity: contact, field: integer_field); v.value = "abc"`; then `expect(v.valid?).to be(false)`, then `expect(v.valid?).to be(false)` AGAIN, then `expect(v.save).to be(false)`; assert `v.errors[:value]` is still present on the third check and `TypedEAV::Value.where(entity: contact, field: integer_field)` is empty (no nil row persisted). Merely observing validity must not change it.
   - PENDING: `invalid?` as the first observation (the Rails form-preflight pattern) followed by `save` - same expectations. Both observation entry points matter.
   - AFFIRMATIVE (not pending): correction by REASSIGNMENT - after the rejected `"abc"`, assign `v.value = 7`; assert `v.valid?` is true, `v.save` succeeds, and the persisted `integer_value` is 7. This locks that the repair makes the flag sticky-until-reassigned, not permanently poisoning the object.

3. "IMPROVEMENT PROGRAM 3: exact-partition Field and Section ordering" (`reorder_within_partition`, `field/base.rb:528-544` and the mirrored `section.rb:111-124`, which reuse the deliberately WIDENED `for_entity` scope as the sibling set). Build ONE fresh unique entity_type per model (e.g. "OrdImpPgm3Fld" and "OrdImpPgm3Sec" - never OrdConcur / OrdPart / any entity_type already used by `field_spec.rb`) and create THREE fixtures in each:
   - fallback/global: `scope: nil, parent_scope: nil`
   - scope-only: `scope: "t1", parent_scope: nil`
   - full tuple: `scope: "t1", parent_scope: "w1"`
   Give every row a known `sort_order`. Then, for each of the three fixtures in turn, PENDING: call a move helper (`move_higher` / `move_to_bottom` / `insert_at`) on that fixture's row and assert the OTHER TWO fixtures' reloaded `sort_order` values are byte-for-byte unchanged. Write this as a Field set and a Section set (Section mirrors Field by deliberate convention). Inline the minimal setup - do NOT try to reuse `make_partition`, which is local to `field_spec.rb`.

4. "IMPROVEMENT PROGRAM 4: one-shot version-group correlation under object reuse" (`pending_version_group_id`, `value.rb:153`, stamped at `lib/typed_eav/bulk_write.rb:261-273`, read at `lib/typed_eav/versioning/subscriber.rb:145` as `value.pending_version_group_id || context[:version_group_id]`, never cleared). Mirror the versioning setup convention used by `spec/lib/typed_eav/bulk_write_per_record_spec.rb` and `spec/models/typed_eav/value_versioning_integration_spec.rb` (including their `:real_commits` / `:event_callbacks` metadata and versioning-config toggling - read those files and copy the convention exactly). Write:
   - PENDING (same-object reuse): run a bulk write, capture the Value OBJECT and its first ValueVersion's `version_group_id` (UUID-A, assert present). Then, OUTSIDE any bulk call and OUTSIDE any `TypedEAV.with_context`, mutate and `save!` the SAME in-memory object; assert the SECOND ValueVersion's `version_group_id` is `nil`.
   - PENDING (same-object reuse under a DIFFERENT context): repeat, but wrap the second save in `TypedEAV.with_context(version_group_id: uuid_b) { ... }`; assert the second version row carries `uuid_b`, not the stale UUID-A.
   - AFFIRMATIVE (not pending): within ONE bulk call, every stamped Value's version row shares the same UUID. This locks bulk correlation so plan 03's repair cannot break it.
   Scope every assertion strictly to cross-call ivar staleness - do NOT assert anything about `BulkWrite.execute` / `execute_pairs` pair-shaped duplicate-instance handling, which is deliberate and already correct.

5. "IMPROVEMENT PROGRAM 5: field/query Integer casting agreement" (`Field::Integer#cast`, `field/integer.rb:17-28`, rejects `bd.frac != 0`; `QueryBuilder` never calls the field for comparison operators). Use `:integer_field`. Write:
   - AFFIRMATIVE (not pending): the write-side contract - `field.cast("3.7") == [nil, true]` and `field.cast(" 7 ") == [7, false]`.
   - PENDING: the query side agrees on rejection - `expect { TypedEAV::QueryBuilder.filter(field, :eq, "3.7") }.to raise_error(ArgumentError)`, and the same for `:gt` with `"3.7"`.
   - PENDING: the query side agrees on normalization - with a stored row whose value is `7`, `QueryBuilder.filter(field, :eq, " 7 ")` matches that row.
   Assert the desired AGREEMENT, not a particular implementation - plan 05 chooses the mechanism.

6. "IMPROVEMENT PROGRAM 6: strict between input shape" (`query_builder.rb:96-102`, only checks `respond_to?(:first)` / `respond_to?(:last)`). Use `:integer_field`. Write:
   - PENDING: `expect { TypedEAV::QueryBuilder.filter(field, :between, [1, 2, 3]) }.to raise_error(ArgumentError, /between/)`, and the same for `[5]` and `[]`.
   - AFFIRMATIVE (not pending): a `Range` (`1..10`) and a well-formed two-element Array (`[1, 10]`) do NOT raise and both match a stored row inside the bounds. This locks that plan 05's repair cannot over-tighten.

7. "IMPROVEMENT PROGRAM 7: full default-domain validation" (`validate_default_value`, `field/base.rb:436-444`, checks only the `cast` invalid flag and never `validate_typed_value`). Write ONE pending example per default-value family, each asserting `expect(field).not_to be_valid` with an error on `:default_value`, plus a paired affirmative example asserting an IN-domain default of the same family still validates:
   - numeric min/max: `:integer_field` with `options: { "min" => 0, "max" => 10 }` and `default_value_meta = { "v" => 999 }`; repeat for a decimal field.
   - Percentage bounds: a Percentage field with a default outside its allowed range.
   - text length/pattern: a string-typed field carrying `min_length` / `max_length` / `pattern` options with a default that violates them (see `spec/models/typed_eav/field/validated_string_spec.rb` for the option keys actually in use).
   - option inclusion (single): `create(:select_field)` - its options are created via `after(:create)`, so it must be created before `allowed_option_values` is non-empty - then `default_value_meta = { "v" => "not_a_real_option" }`.
   - option inclusion (multi): the MultiSelect equivalent, with a default array containing a non-member.
   - array size/content: a text/integer array field with `min_size`/`max_size` options and a default array that violates the size bound, plus one whose element content is invalid for the element type.
   - Currency completeness: a Currency field whose default hash has an amount but no currency (and vice versa).
   - Currency allowed currency: a Currency field with `allowed_currencies` set and a default whose currency is outside the list.
   Use existing factories; where a family has no factory, build inline from the existing base field factory with `options:` overrides. Do NOT add or edit any factory.
   Also write one AFFIRMATIVE (not pending) example: a NEW unsaved Select field with no options yet and a default set must not be failed spuriously - its domain is not yet determinable. This locks the carve-out plan 04 must honor.

Before writing each PENDING reproduction, RUN it once WITHOUT the `pending(...)` line to confirm it actually fails today. If any one of them PASSES without the pending marker, STOP immediately: do not weaken the assertion, do not force it pending. Report which case passed, the exact assertion, and its output.

STOP CONDITIONS (report and halt, do not work around): any edit to production code, factories, spec helpers, migrations, docs, bench, or .vbw-planning; any pre-existing unrelated failure surfacing in the full run; any suspicion of nondeterminism (order-dependent or timing-dependent pass/fail); an already-correct behavior that would have to be force-pended; a dirty worktree at start.
  </action>
  <verify>
Confirm a clean worktree BEFORE starting: `git status --porcelain` (must be empty).

All commands run through Ruby 3.4.4:
1. Targeted: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb --format documentation` - every reproduction reports as PENDING (failing as expected), every affirmative guard-rail example PASSES; zero failures, zero examples unexpectedly passing.
2. Coverage audit of the matrix: the run's documentation output contains at least one example for each of the five class-1 pending states, both class-2 observation entry points plus the reassignment case, three-by-two Field/Section partition fixtures for class 3, both class-4 reuse cases plus the within-bulk affirmative, both class-5 directions, all four class-6 shapes, and one example per class-7 family.
3. Determinism: run the targeted file three times with `--seed random`; identical pending/passing counts every time.
4. Full suite: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures; the pre-existing 1189 examples are otherwise unchanged.
5. Lint: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop spec/regressions/improvement_program_phase_0_spec.rb` - no offenses.
6. Whitespace: `git diff --check` - no output.
7. Scope guard: `git status --porcelain` lists ONLY `spec/regressions/improvement_program_phase_0_spec.rb`.
8. Release guard: `git tag --points-at HEAD` is empty; `git diff --stat -- lib/typed_eav/version.rb CHANGELOG.md` is empty; no push was performed.
  </verify>
  <done>
`spec/regressions/improvement_program_phase_0_spec.rb` exists with seven `IMPROVEMENT PROGRAM N` describe blocks covering the full reproduction matrix above. All eight verification checks pass. Exactly one commit: `test(regressions): add pending reproductions for seven correctness classes`.

Then STOP and report for Sol review - do not begin plan 02.
  </done>
</task>
</tasks>
<verification>
1. `git status --porcelain` before the commit lists exactly one path: `spec/regressions/improvement_program_phase_0_spec.rb`.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb` reports 0 failures and 0 unexpectedly-passing pending examples.
3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` reports 0 failures.
4. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop spec/regressions/improvement_program_phase_0_spec.rb` reports no offenses.
5. `git diff --check` produces no output.
6. `grep -c "IMPROVEMENT PROGRAM" spec/regressions/improvement_program_phase_0_spec.rb` is at least 7.
7. `git log -1 --pretty=%s` matches the conventional format `test(regressions): ...`.
8. `git tag --points-at HEAD` is empty and `git log origin/main..HEAD` shows the commit is unpushed.
</verification>
<success_criteria>
- Pending behavioral reproductions exist for all seven confirmed correctness classes across the full matrix in this plan's must_haves, added with no production fix.
- Each reproduction demonstrably fails today (RSpec reports it as pending/failing-as-expected, never as unexpectedly passing), and the affirmative guard-rail examples pass now.
- Only the single allowed file changed; full RSpec, targeted RSpec, RuboCop, and `git diff --check` are green on Ruby 3.4.4.
- No push, tag, version bump, publication, or release occurred; the plan halts for Sol review before the next writer.
</success_criteria>
<output>
01-SUMMARY.md
</output>
</output>

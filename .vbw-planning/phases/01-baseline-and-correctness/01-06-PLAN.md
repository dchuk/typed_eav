---
phase: 1
plan: 06
title: Gate 1 - full verification sweep and contract audit
type: execute
wave: 6
depends_on: ["01-05"]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - "docs/improvement-program.md"
forbidden_commands: ["git push", "gem push", "scripts/bump-version.sh", "rake release", "git tag"]
must_haves:
  truths:
    - "Gate 1 evidence is real: targeted RSpec, full RSpec, RuboCop, and `git diff --check` were all run on Ruby 3.4.4 via /Users/darrindemchuk/.rbenv/versions/3.4.4/bin and all four are green."
    - "All seven Phase 0A reproductions pass and no `pending(` marker remains in spec/regressions/improvement_program_phase_0_spec.rb."
    - "Public API, tenancy, transactions, validation, and auditability contracts are unchanged versus the Phase 0B baseline: no public method removed or renamed, no signature change, no migration added, no schema or index change."
    - "No version bump, tag, push, publication, release, or CHANGELOG version entry occurred anywhere in Phase 1."
    - "The only file this plan writes is docs/improvement-program.md, recording the Gate 1 results."
  artifacts:
    - path: "docs/improvement-program.md"
      provides: "Recorded Gate 1 results: the four green checks, the seven repaired classes with their commits, and the confirmed-unchanged contract surface"
      contains: "Gate 1"
  key_links:
    - from: "docs/improvement-program.md"
      to: "spec/regressions/improvement_program_phase_0_spec.rb"
      via: "Gate 1 record cites the seven now-passing reproductions"
    - from: "docs/improvement-program.md"
      to: "bench/results/phase-0-baseline.json"
      via: "Gate 1 record confirms the captured schema and index definitions are unchanged by the repairs"
---
<objective>
Close Phase 1. Run the Gate 1 verification sweep, audit that the preserved contracts really are preserved, and record the result in `docs/improvement-program.md`. No production code changes: if the sweep finds a failure, STOP and report it rather than repairing it inside the gate.
</objective>
<context>
@.vbw-planning/phases/01-baseline-and-correctness/01-RESEARCH.md
@.vbw-planning/ROADMAP.md
@.vbw-planning/phases/01-baseline-and-correctness/01-02-PLAN.md

Gate 1 (ROADMAP Phase 1): targeted RSpec + full RSpec + RuboCop + `git diff --check` green on Ruby 3.4.4; all seven reproductions passing; public API, tenancy, transactions, validation, and auditability contracts unchanged.

RESEARCH.md notes that the pre-repair baseline was verified only by `rspec --dry-run` (1189 examples collect cleanly), never by a full execution - so a full green run here is the first end-to-end confirmation and is load-bearing evidence. RESEARCH.md section C is the public API surface to audit against; `docs/improvement-program.md` (written in plan 02) is the committed copy of it.

Binding: no version bump, tag, push, publication, release, or CHANGELOG version entry anywhere in this milestone.
</context>
<tasks>
<task type="auto">
  <name>Run the Gate 1 verification sweep and contract audit (no file changes)</name>
  <files>
    (none - verification only, do not commit this task)
  </files>
  <action>
Run every check below on Ruby 3.4.4 and capture the exact output for the next task's record. This task makes NO file changes and produces NO commit.

Verification sweep:
  1. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb --format documentation` - all seven reproductions pass, 0 pending, 0 failures.
  2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec --format progress` - full suite, 0 failures. Record the final example / failure / pending counts and compare the example count against the 1189 baseline (it should have grown by exactly the reproductions added in plan 01).
  3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop` - whole repo, no offenses.
  4. `git diff --check` - no output. Also confirm `git status --porcelain` is clean.
  5. `grep -c "pending(" spec/regressions/improvement_program_phase_0_spec.rb` returns 0.

Contract audit (read-only):
  6. Public API: confirm every symbol listed in `docs/improvement-program.md`'s public API section still exists with an unchanged signature - use LSP `workspaceSymbol` / `hover` or a Ruby `respond_to?` probe through the dummy environment rather than eyeballing. Explicitly confirm `TypedEAV::Value#pending_version_group_id`, `TypedEAV::QueryBuilder.filter` / `.entity_ids`, `Field::Base#cast` / `#validate_typed_value` / the five ordering helpers, and `Section#for_entity` are all still present.
  7. Schema: `git diff --stat <phase-start-commit>..HEAD -- db/migrate` is empty - Phase 1 added no migration, so the schema and index definitions captured in `bench/results/phase-0-baseline.json` still hold. Re-run the baseline helper and confirm its `schema` section is unchanged from the committed JSON (compare only the `schema` section; the `runtime_snapshot` section legitimately varies).
  8. Tenancy: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/review_round_4_parent_scope_spec.rb` and the scope/partition specs pass - two-axis, fail-closed scoping is intact.
  9. Transactions and auditability: the versioning and bulk specs pass (already covered by the full run) and `lib/typed_eav/bulk_write.rb` is untouched by Phase 1 - `git diff --stat <phase-start-commit>..HEAD -- lib/typed_eav/bulk_write.rb` is empty.
  10. Release surface: `git diff --stat <phase-start-commit>..HEAD -- lib/typed_eav/version.rb CHANGELOG.md` is empty; `git tag --points-at HEAD` is empty; no push occurred.

If ANY check fails, STOP: do not repair it here. Report the failing check, its exact output, and which plan/task owns the affected file so the orchestrator can route a fix.
  </action>
  <verify>
All ten checks above produce their stated result. Working tree remains clean (`git status --porcelain` empty) and no commit was created by this task.
  </verify>
  <done>
Every Gate 1 check is green, the contract audit found no drift, and the exact command outputs are captured for the record task. No files changed.
  </done>
</task>
<task type="auto">
  <name>Record Gate 1 results in docs/improvement-program.md</name>
  <files>
    docs/improvement-program.md
  </files>
  <action>
Fill in the Gate 1 placeholder section created in plan 02 with the evidence gathered in the previous task:
  - the four gate checks (targeted RSpec, full RSpec, RuboCop, `git diff --check`), each with the exact command run on Ruby 3.4.4 and its result, including the full-suite example / failure / pending counts and the delta from the 1189 dry-run baseline;
  - the seven correctness classes as a table: class number, short title, the file(s) repaired, and the commit subject that repaired it (`git log --oneline` for the phase gives the subjects);
  - the contract-audit result: public API unchanged (naming the specific symbols probed), no migration added, `bench/results/phase-0-baseline.json`'s `schema` section unchanged, tenancy/transactions/validation/auditability specs green;
  - an explicit line stating that no version bump, tag, push, publication, release, or CHANGELOG version entry was made in Phase 1.

Do not restate the baseline facts already recorded in plan 02 - reference them. Do not edit any other file.
  </action>
  <verify>
1. `grep -q "Gate 1" docs/improvement-program.md` succeeds and the section contains the four commands and their results.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop docs` - no offenses.
3. `git status --porcelain` lists only `docs/improvement-program.md`.
4. `git diff --check` - no output.
5. `git diff --stat -- lib/typed_eav/version.rb CHANGELOG.md` is empty.
  </verify>
  <done>
The Gate 1 section of `docs/improvement-program.md` records all four green checks, the seven repaired classes with their commits, and the contract audit. Exactly one commit exists: `docs(improvement-program): record Gate 1 results for phase 1`.
  </done>
</task>
</tasks>
<verification>
1. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb` - seven reproductions pass, 0 pending, 0 failures.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures.
3. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop` - no offenses.
4. `git diff --check` produces no output and `git status --porcelain` is clean after the commit.
5. `grep -q "Gate 1" docs/improvement-program.md` succeeds.
6. `git diff --stat` across the phase shows no change to `db/migrate/`, `lib/typed_eav/version.rb`, `CHANGELOG.md`, `Gemfile`, `Gemfile.lock`, or `typed_eav.gemspec`.
7. `git tag --points-at HEAD` is empty.
</verification>
<success_criteria>
- Gate 1 passes: targeted RSpec, full RSpec, RuboCop, and `git diff --check` are green on Ruby 3.4.4; all seven reproductions pass.
- Public API, tenancy, transactions, validation, and auditability contracts are audited and confirmed unchanged.
- The Gate 1 result is recorded in `docs/improvement-program.md`, with no version bump, tag, push, publication, or release anywhere in the phase.
</success_criteria>
<output>
06-SUMMARY.md
</output>

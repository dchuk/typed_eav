---
phase: 1
plan: 06
title: Gate 1 - full verification sweep, packaging checks, and contract audit
type: execute
wave: 6
depends_on: ["01-05"]
cross_phase_deps: []
autonomous: false
effort_override: thorough
skills_used: []
files_modified:
  - "docs/improvement-program.md"
forbidden_commands: ["git push", "git tag", "gem push", "scripts/bump-version.sh", "rake release", "gem release", "bundle exec rake release"]
must_haves:
  truths:
    - "No push, no tag, no version bump, no publication, no release: `git push`, `git tag`, `gem push`, `scripts/bump-version.sh`, and any `rake release` form are forbidden for the entire plan. No CHANGELOG version entry is added."
    - "Gate 1 runs the FULL spec suite on Ruby 3.4.4 and it is green - not a targeted subset."
    - "Gate 1 runs the transient-state and callback-sensitive specs REPEATEDLY (at least five consecutive runs with randomized seeds) and they are stable: the pending-state, invalid-cast observational-validation, version-group correlation, event-dispatch, and reorder :concurrency specs must produce identical results every run."
    - "Gate 1 runs the compatibility contract suite, and the compatibility matrix to the extent it is available locally; lanes that cannot be executed locally are recorded as NOT RUN with the reason, never reported as passing."
    - "Gate 1 runs a migration-from-scratch check: an empty database migrated by the engine + dummy migration path with no schema load, matching how CI and spec_helper bootstrap."
    - "Gate 1 runs the consumer-install smoke (`script/consumer_install_smoke`) and the package contents check (`script/package_contents_check`) without publishing anything."
    - "Gate 1 runs RuboCop across the whole repo with no offenses, and the diff checks `git diff --check` plus a clean `git status --porcelain`."
    - "Gate 1 runs a contract audit proving public API, tenancy, transactions, validation, and auditability are unchanged versus the Phase 0B baseline: no public method removed or renamed, no signature change, no migration added, no schema or index change."
    - "STOP ON FAILURE: any failing check halts the gate. Nothing is repaired inside this plan; the failure, its exact output, and the owning plan/file are reported so the orchestrator can route a fix."
    - "All seven correctness classes are repaired and no `pending(` marker remains in spec/regressions/improvement_program_phase_0_spec.rb."
    - "The only file this plan writes is docs/improvement-program.md, recording the Gate 1 results and updating the status tracker."
    - "Each task completion pauses for Sol review: after each task, report the evidence and STOP before starting the next task; Gate 1 sign-off is Sol's, not the writer's."
  artifacts:
    - path: "docs/improvement-program.md"
      provides: "Recorded Gate 1 results: every check with its exact command and output, the seven repaired classes with their commits, the packaging/compatibility results, and the confirmed-unchanged contract surface"
      contains: "Gate 1"
  key_links:
    - from: "docs/improvement-program.md"
      to: "spec/regressions/improvement_program_phase_0_spec.rb"
      via: "Gate 1 record cites the now-passing reproductions for all seven classes"
    - from: "docs/improvement-program.md"
      to: "bench/results/phase-0-baseline.json"
      via: "Gate 1 record confirms the captured schema and index definitions are unchanged by the repairs"
---
<objective>
Close Phase 1. Run the full Gate 1 sweep - specs, repeated transient-state runs, compatibility, migration-from-scratch, consumer-install and packaging smoke, lint, diff checks - then audit that the preserved contracts really are preserved, and record the result in `docs/improvement-program.md`. No production code changes: if any check fails, STOP and report rather than repairing it inside the gate.
</objective>
<context>
@.vbw-planning/phases/01-baseline-and-correctness/01-RESEARCH.md
@.vbw-planning/ROADMAP.md
@.vbw-planning/phases/01-baseline-and-correctness/01-02-PLAN.md

Gate 1 (ROADMAP Phase 1): full and targeted RSpec, repeated transient-state/callback runs, compatibility, migration-from-scratch, consumer-install smoke, RuboCop, diff checks, and a contract audit - all green on Ruby 3.4.4; all seven reproductions passing; public API, tenancy, transactions, validation, and auditability contracts unchanged.

RESEARCH.md notes the pre-repair baseline was verified only by `rspec --dry-run` (1189 examples collect cleanly), never by a full execution - so a full green run is load-bearing evidence. RESEARCH.md section C is the public API surface to audit against; `docs/improvement-program.md` (written in plan 02) is the committed copy of it.

Local tooling confirmed present: `script/consumer_install_smoke`, `script/package_contents_check`, `spec/integration/compatibility_contract_spec.rb`, `spec/integration/package_artifact_spec.rb`, `spec/integration/release_workflow_spec.rb`, and `.github/compatibility.json` as the matrix source of truth. CI migrates from scratch rather than loading a schema; reproduce that shape locally.

Binding: no version bump, tag, push, publication, release, or CHANGELOG version entry anywhere in this milestone.
</context>
<tasks>
<task type="auto">
  <name>Gate 1 sweep A - full suite, repeated transient-state runs, lint, diff checks</name>
  <files>
    (none - verification only, no commit)
  </files>
  <action>
Run every check below on Ruby 3.4.4 and capture the exact output for the record task. This task makes NO file changes and produces NO commit. STOP at the first failure and report.

  1. Targeted reproductions: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb --format documentation` - every reproduction across all seven classes passes, 0 pending, 0 failures.
  2. Full suite: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec --format progress` - 0 failures. Record the final example / failure / pending counts and the delta from the 1189 dry-run baseline.
  3. REPEATED TRANSIENT-STATE AND CALLBACK RUNS - the highest-risk area of this phase, because three of the seven defects were transient-ivar lifecycle bugs and two repairs touch callback ordering. Run each of these FIVE consecutive times with `--seed random`, recording every run's result:
       - `spec/regressions/improvement_program_phase_0_spec.rb`
       - `spec/models/typed_eav/value_spec.rb`
       - `spec/models/typed_eav/value_versioning_integration_spec.rb spec/lib/typed_eav/versioning/subscriber_spec.rb`
       - `spec/lib/typed_eav/bulk_write_per_record_spec.rb spec/lib/typed_eav/bulk_operations_spec.rb`
       - `spec/models/typed_eav/value_event_spec.rb spec/models/typed_eav/field_event_spec.rb spec/lib/typed_eav/event_dispatcher_spec.rb spec/lib/typed_eav/event_context_spec.rb`
       - `spec/models/typed_eav/field_spec.rb` (includes the `:concurrency` context, which runs real threads with `use_transactional_tests = false`)
     Every run must produce identical pass/fail/pending counts. A single divergent run is a failure of the gate - report it as suspected nondeterminism with both outputs; do not re-run until it passes.
  4. Whole-repo lint: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop` - no offenses.
  5. Diff checks: `git diff --check` produces no output; `git status --porcelain` is clean.
  6. `grep -c "pending(" spec/regressions/improvement_program_phase_0_spec.rb` returns 0.

If ANY check fails, STOP: do not repair it here. Report the failing check, its exact output, and which plan/task owns the affected file so the orchestrator can route a fix.
  </action>
  <verify>
All six checks produce their stated result, including five identical consecutive runs for each of the six transient-state/callback groups. Working tree remains clean and no commit was created.
  </verify>
  <done>
Every sweep-A check is green and the repeated runs are stable. Outputs captured for the record task. No files changed.

Then STOP and report for Sol review before starting the next task.
  </done>
</task>
<task type="auto">
  <name>Gate 1 sweep B - compatibility, migration-from-scratch, consumer-install and packaging smoke</name>
  <files>
    (none - verification only, no commit)
  </files>
  <action>
Run the packaging and environment checks. No file changes, no commit, no publication of any kind. STOP at the first failure and report.

  1. COMPATIBILITY CONTRACT: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/integration/compatibility_contract_spec.rb spec/integration/documentation_consistency_spec.rb` - 0 failures. Then check the matrix from `.github/compatibility.json` against what is actually runnable on this machine: the local toolchain is Ruby 3.4.4 / Rails 8.1.3 / PG 17.7, which corresponds to no single CI lane exactly. Record, lane by lane (floor 3.3/7.2/PG15, middle 3.4/8.0/PG16, ceiling 4.0/8.1/PG18), whether it was RUN, PARTIALLY RUN, or NOT RUN LOCALLY with the reason. A lane that cannot be executed locally is recorded as NOT RUN - never as passing, and never inferred from another lane's result.
  2. MIGRATION FROM SCRATCH: drop/recreate a scratch TEST database and migrate it from empty using the engine `db/migrate` plus `spec/dummy/db/migrate` paths, exactly the way `spec/spec_helper.rb` and the CI "Create database and run migrations" step do - no `db:schema:load`. Confirm every migration applies cleanly in order, including `20260712000000_enforce_parent_scope_invariant.rb`'s two-step `validate_check_constraint` and its orphan-row guard. Record the migration list and the final `schema_migrations` state. This operates on a TEST/scratch database only - never a development or production database, and only through migration files, never ad-hoc DDL.
  3. CONSUMER-INSTALL SMOKE: run `script/consumer_install_smoke` per its own documented usage and record the result. It must complete without publishing, pushing, or tagging anything.
  4. PACKAGE CONTENTS: run `script/package_contents_check` and `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/integration/package_artifact_spec.rb spec/integration/release_workflow_spec.rb` - 0 failures. Building a gem artifact locally for inspection is allowed; `gem push` and any release task are not.
  5. RELEASE SURFACE GUARD: `git diff --stat <phase-start-commit>..HEAD -- lib/typed_eav/version.rb CHANGELOG.md` is empty; `git tag --points-at HEAD` is empty; `git log origin/main..HEAD` confirms nothing was pushed.

If ANY check fails, STOP and report the failing check, its exact output, and the owning plan/file.
  </action>
  <verify>
All five checks produce their stated result. The compatibility record names every lane with an explicit RUN / PARTIALLY RUN / NOT RUN verdict and reason. Migration-from-scratch touched only a scratch test database. No artifact was published, pushed, or tagged. No commit was created.
  </verify>
  <done>
Every sweep-B check is green (or explicitly recorded as NOT RUN LOCALLY with a reason), the release surface is untouched, and outputs are captured for the record task. No files changed.

Then STOP and report for Sol review before starting the next task.
  </done>
</task>
<task type="auto">
  <name>Contract audit and Gate 1 record in docs/improvement-program.md</name>
  <files>
    docs/improvement-program.md
  </files>
  <action>
First the CONTRACT AUDIT (read-only), then write the record.

AUDIT:
  a. PUBLIC API: confirm every symbol listed in `docs/improvement-program.md`'s public API section still exists with an unchanged signature - use LSP `workspaceSymbol` / `hover` or a `respond_to?` / `method(:x).arity` probe through the dummy environment rather than eyeballing. Explicitly confirm `TypedEAV::Value#pending_version_group_id`, `TypedEAV::QueryBuilder.filter` / `.entity_ids`, `Field::Base#cast` / `#validate_typed_value` / the five ordering helpers, `Field::Base.for_entity` and `Section.for_entity` (still widened), and the newly added `for_exact_partition` / `cast_query_operand` as ADDITIVE (no removal, no renaming, no new required argument on any existing method).
  b. SCHEMA: `git diff --stat <phase-start-commit>..HEAD -- db/migrate` is empty - Phase 1 added no migration. Re-run the baseline helper to a scratch path and confirm its `schema` section is byte-identical to the committed `bench/results/phase-0-baseline.json`; the `runtime_snapshot` section legitimately varies.
  c. TENANCY: `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/review_round_4_parent_scope_spec.rb spec/lib/typed_eav/scoping_spec.rb spec/lib/typed_eav/scope_tuple_spec.rb spec/lib/typed_eav/partition_spec.rb` - 0 failures; two-axis, fail-closed scoping and three-way collision precedence intact.
  d. TRANSACTIONS AND AUDITABILITY: the versioning and bulk specs pass (covered by the full run) and `lib/typed_eav/bulk_write.rb` is untouched - `git diff --stat <phase-start-commit>..HEAD -- lib/typed_eav/bulk_write.rb` is empty.
  e. VALIDATION: the field and value validation specs pass and the seven repaired behaviors are each covered by a now-passing reproduction.
Any drift is a gate failure: STOP and report, do not adjust the audit to fit.

RECORD: fill in the Gate 1 placeholder section of `docs/improvement-program.md` with:
  - every Gate 1 check from sweeps A and B, each with the exact command run on Ruby 3.4.4 and its result, including the full-suite counts and the delta from the 1189 dry-run baseline, and the five-run stability results for each transient-state/callback group;
  - the compatibility lane table with RUN / PARTIALLY RUN / NOT RUN LOCALLY and the reason per lane;
  - the migration-from-scratch result and the consumer-install / package-contents results;
  - the seven correctness classes as a table: class number, short title, the file(s) repaired, and the commit subject that repaired it (`git log --oneline` for the phase gives the subjects);
  - the contract-audit result, naming the specific symbols probed and stating that `for_exact_partition` and `cast_query_operand` are additive;
  - an explicit line stating that no version bump, tag, push, publication, release, or CHANGELOG version entry was made in Phase 1;
  - updates to the STATUS TRACKER table: set the status, decision, evidence, commit, and follow-up columns for every Phase 1 plan and task row, including Gate 0 and Gate 1.
Do not restate the baseline facts already recorded in plan 02 - reference them. Do not edit any other file. Gate 1 sign-off itself belongs to Sol: record the evidence and mark the gate as awaiting sign-off rather than declaring it passed on your own authority.
  </action>
  <verify>
1. `grep -q "Gate 1" docs/improvement-program.md` succeeds and the section contains every sweep-A and sweep-B check with its command and result.
2. The compatibility lane table lists all three lanes with an explicit verdict and reason.
3. The status tracker rows for all Phase 1 plans and tasks (including Gate 0 and Gate 1) have their status, evidence, and commit columns filled.
4. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop docs` - no offenses (or no Ruby files to inspect).
5. `git status --porcelain` lists only `docs/improvement-program.md`; `git diff --check` produces no output.
6. `git diff --stat -- lib/typed_eav/version.rb CHANGELOG.md` is empty; `git tag --points-at HEAD` is empty; nothing was pushed.
  </verify>
  <done>
The contract audit found no drift and the Gate 1 section records every check, the compatibility lane verdicts, the packaging results, the seven repaired classes with their commits, and the updated status tracker. Exactly one commit exists: `docs(improvement-program): record Gate 1 results for phase 1`.

Then STOP and hand Gate 1 to Sol for sign-off.
  </done>
</task>
</tasks>
<verification>
1. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec spec/regressions/improvement_program_phase_0_spec.rb` - all reproductions pass, 0 pending, 0 failures.
2. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rspec` - 0 failures.
3. Five consecutive randomized-seed runs of each transient-state/callback group produce identical results.
4. `/Users/darrindemchuk/.rbenv/versions/3.4.4/bin/bundle exec rubocop` - no offenses.
5. Migration-from-scratch on a scratch test database applies every migration cleanly; `script/consumer_install_smoke` and `script/package_contents_check` succeed.
6. `git diff --check` produces no output and `git status --porcelain` is clean after the commit.
7. `grep -q "Gate 1" docs/improvement-program.md` succeeds and the compatibility lane table is present with per-lane verdicts.
8. `git diff --stat` across the phase shows no change to `db/migrate/`, `lib/typed_eav/version.rb`, `CHANGELOG.md`, `Gemfile`, `Gemfile.lock`, or `typed_eav.gemspec`.
9. `git tag --points-at HEAD` is empty and `git log origin/main..HEAD` confirms nothing was pushed.
</verification>
<success_criteria>
- Gate 1 passes: full RSpec, targeted reproductions, five-run transient-state/callback stability, compatibility contract plus the locally-runnable matrix (with NOT RUN lanes explicitly recorded), migration-from-scratch, consumer-install and package-contents smoke, RuboCop, and diff checks are all green on Ruby 3.4.4.
- Any failure halts the gate and is reported with its owning plan/file rather than repaired inside the gate.
- Public API, tenancy, transactions, validation, and auditability contracts are audited and confirmed unchanged; `for_exact_partition` and `cast_query_operand` are confirmed additive.
- The Gate 1 result and the updated status tracker are recorded in `docs/improvement-program.md`, with no version bump, tag, push, publication, or release anywhere in the phase.
</success_criteria>
<output>
06-SUMMARY.md
</output>
</output>

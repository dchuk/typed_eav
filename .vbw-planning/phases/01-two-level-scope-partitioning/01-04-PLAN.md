---
phase: 1
plan: 04
title: Section — parent_scope partition + uniqueness + invariant
type: execute
wave: 1
depends_on: ["01-01"]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: [rails-model-generator]
files_modified:
  - app/models/typed_eav/section.rb
forbidden_commands: []
must_haves:
  truths:
    - "Section.for_entity accepts both scope: and parent_scope: kwargs (both default nil)"
    - "for_entity scope expands to where(entity_type:, scope: [scope, nil].uniq, parent_scope: [parent_scope, nil].uniq)"
    - "AR uniqueness validator on :code uses scope: %i[entity_type scope parent_scope] (was %i[entity_type scope])"
    - "New validate :validate_parent_scope_invariant rejects parent_scope.present? && scope.nil? — symmetric to Field::Base"
    - "Existing API surface preserved: Section.for_entity(et, scope: s) still works for callers that don't pass parent_scope"
    - "No new dependencies; mirrors plan 03's Field::Base changes inline-duplicated per CONTEXT.md"
    - "Single commit: feat(section): partition by (entity_type, scope, parent_scope) and reject orphan parents"
  artifacts:
    - path: "app/models/typed_eav/section.rb"
      provides: "Three-key partition support for Section"
      contains: "parent_scope: [parent_scope, nil].uniq"
    - path: "app/models/typed_eav/section.rb"
      provides: "Orphan-parent invariant validator"
      contains: "validate_parent_scope_invariant"
  key_links:
    - {from: "app/models/typed_eav/section.rb", to: "app/models/typed_eav/field/base.rb", via: "inline-duplicated symmetric logic — Field::Base mirrors these changes; both must stay in lockstep"}
    - {from: "app/models/typed_eav/section.rb", to: "db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb", via: "AR uniqueness validator scope key matches the new idx_te_sections_uniq_scoped index columns"}
---
<objective>
Mirror the Field::Base parent_scope partition changes onto `Section`. Per CONTEXT.md, the symmetric scope/parent_scope logic is intentionally inline-duplicated across these two files (no `Scopable` concern this phase). The two implementations must stay symmetric — diff between this plan and plan 03 should be limited to the differing uniqueness key (`:code` for Section vs `:name` for Field) and the file path.

The orphan-parent invariant applies identically: a section with `scope=nil` cannot have `parent_scope` set. Section is queried directly from admin/form code (it is NOT wired through `ClassQueryMethods` in `has_typed_eav`), so callers pass `scope:` and `parent_scope:` kwargs explicitly to `Section.for_entity`.
</objective>
<context>
@.vbw-planning/phases/01-two-level-scope-partitioning/01-CONTEXT.md
@.vbw-planning/phases/01-two-level-scope-partitioning/01-RESEARCH.md
@app/models/typed_eav/section.rb
@db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb

Today (RESEARCH §6):
- Line 13: `validates :code, presence: true, uniqueness: { scope: %i[entity_type scope] }`
- Lines 20–22:
    scope :for_entity, lambda { |entity_type, scope: nil|
      where(entity_type: entity_type, scope: [scope, nil].uniq)
    }

This file is touched ONLY by this plan in wave 1 — no overlap with plans 02 or 03.
</context>
<tasks>
<task type="auto">
  <name>Update Section.for_entity scope to accept and apply parent_scope kwarg</name>
  <files>
    app/models/typed_eav/section.rb
  </files>
  <action>
Replace the existing for_entity scope:

  scope :for_entity, lambda { |entity_type, scope: nil, parent_scope: nil|
    where(
      entity_type: entity_type,
      scope: [scope, nil].uniq,
      parent_scope: [parent_scope, nil].uniq,
    )
  }

Semantics are identical to plan 03's Field::Base.for_entity (intentional symmetry):
- `Section.for_entity(et)` → globals only (scope nil, parent_scope nil).
- `Section.for_entity(et, scope: "t1")` → scope "t1" + globals (parent_scope IS NULL on returned rows).
- `Section.for_entity(et, scope: "t1", parent_scope: "p1")` → matches the "t1"/"p1" partition plus its globals on either axis (the orphan-parent invariant constrains valid combinations).

Update the inline doc comment to reflect the three-key set semantics. The comment above the scope today (lines 17-19) explaining "Mirror Field::Base.for_entity..." should be extended with one sentence noting parent_scope expansion.

Public method signature stays backwards-compatible — callers passing only `scope:` continue to work unchanged.
  </action>
  <verify>
From IRB:
  TypedEAV::Section.for_entity("Contact").to_sql # contains scope IS NULL and parent_scope IS NULL
  TypedEAV::Section.for_entity("Contact", scope: "t1").to_sql # contains scope IN ('t1', NULL) and parent_scope IS NULL
  TypedEAV::Section.for_entity("Contact", scope: "t1", parent_scope: "p1").to_sql # contains scope IN ('t1', NULL) and parent_scope IN ('p1', NULL)
  </verify>
  <done>
Section.for_entity accepts parent_scope: kwarg defaulting to nil; doc comment mentions the parent_scope axis.
  </done>
</task>
<task type="auto">
  <name>Update Section AR uniqueness validator to include parent_scope</name>
  <files>
    app/models/typed_eav/section.rb
  </files>
  <action>
Change line 13 from:

  validates :code, presence: true, uniqueness: { scope: %i[entity_type scope] }

to:

  validates :code, presence: true, uniqueness: { scope: %i[entity_type scope parent_scope] }

This aligns the AR-level uniqueness check with the new triple-aware paired-partial unique indexes added in plan 01 (`idx_te_sections_uniq_scoped` on `[entity_type, code, scope, parent_scope]` and `idx_te_sections_uniq_global` on `[entity_type, code]` WHERE scope IS NULL).

The unique key is `code`, not `name` — Section's identity-key is its admin code, while Field's is its `name`. That's the only structural difference between this plan's task and plan 03's task 2.
  </action>
  <verify>
From IRB:
  s_a = TypedEAV::Section.create!(name: "S", code: "x", entity_type: "Contact", scope: "t1", parent_scope: "p1")
  s_b = TypedEAV::Section.new(name: "S2", code: "x", entity_type: "Contact", scope: "t1", parent_scope: "p1")
  s_b.valid? # false; s_b.errors[:code] non-empty
  s_c = TypedEAV::Section.new(name: "S3", code: "x", entity_type: "Contact", scope: "t1", parent_scope: "p2")
  s_c.valid? # true (different parent_scope)
  </verify>
  <done>
Validator scope key is %i[entity_type scope parent_scope]; manual REPL test shows duplicates within (entity_type, code, scope, parent_scope) are rejected; differing parent_scope allows the same (entity_type, code, scope).
  </done>
</task>
<task type="auto">
  <name>Add validate_parent_scope_invariant on Section (mirrors Field::Base)</name>
  <files>
    app/models/typed_eav/section.rb
  </files>
  <action>
Add a validation declaration:

  validate :validate_parent_scope_invariant

And implement as a private method at the end of the class:

  private

  # Orphan-parent invariant: a section with `scope` blank cannot have a
  # `parent_scope` set. Mirrors Field::Base#validate_parent_scope_invariant —
  # CONTEXT.md decision: inline-duplicate across the two files (not a shared
  # concern this phase) so each file is self-contained.
  def validate_parent_scope_invariant
    return if parent_scope.blank?
    return if scope.present?

    errors.add(:parent_scope, "cannot be set when scope is blank")
  end

The implementation is byte-for-byte identical to plan 03's Field::Base validator. That's intentional — when the rule of three triggers (a third caller, or divergence between Section and Field), the future extraction is mechanical.

Section.rb today has no `private` section (it's a small file with only public scopes/validators), so adding `private` cleanly at the end of the class is the right shape. Keep the new method as the only private member for now.
  </action>
  <verify>
From IRB:
  bad = TypedEAV::Section.new(name: "S", code: "x", entity_type: "Contact", scope: nil, parent_scope: "p1")
  bad.valid? # false; bad.errors[:parent_scope] non-empty
  ok1 = TypedEAV::Section.new(name: "S", code: "y", entity_type: "Contact", scope: nil, parent_scope: nil)
  ok1.valid? # true
  ok2 = TypedEAV::Section.new(name: "S", code: "z", entity_type: "Contact", scope: "t1", parent_scope: nil)
  ok2.valid? # true
  ok3 = TypedEAV::Section.new(name: "S", code: "w", entity_type: "Contact", scope: "t1", parent_scope: "p1")
  ok3.valid? # true
  </verify>
  <done>
validate_parent_scope_invariant declared and implemented; private section added; manual REPL exercises all four state combinations correctly.
  </done>
</task>
<task type="auto">
  <name>Run section unit specs and commit</name>
  <files>
    app/models/typed_eav/section.rb
  </files>
  <action>
Run `bin/rspec spec/models/typed_eav/section_and_option_spec.rb` (or whatever file currently exercises Section — the existing scoping_spec covers Section.for_entity at lines 297-312; that subset will be updated in plan 06).

The existing scoping_spec describe block "Section#for_entity" at lines 297-312 makes assertions like:
  result = TypedEAV::Section.for_entity("Contact", scope: "t1")
  expect(result).to include(scoped_section, global_section)
These assertions continue to hold because parent_scope defaults to nil on the existing fixtures (column was added by plan 01 with default nil) and `[nil, nil].uniq == [nil]` so the existing partition behavior is preserved.

If a section unit-spec asserts a specific error format that changes due to the broader uniqueness scope, do NOT modify the spec — flag for plan 06.

Commit message: `feat(section): partition by (entity_type, scope, parent_scope) and reject orphan parents`. Single commit, one file.

Do NOT bump version, push, or update CHANGELOG.
  </action>
  <verify>
`bin/rspec spec/models/typed_eav/` exit code 0 (or pre-existing failures only). `git log --oneline -1` shows the commit. `git diff HEAD~1 --stat` shows exactly one file (`app/models/typed_eav/section.rb`).
  </verify>
  <done>
Single atomic commit; section unit specs run; no spec files modified by this plan; no version bump or CHANGELOG edit.
  </done>
</task>
</tasks>
<verification>
1. `Section.for_entity(...)` accepts both `scope:` and `parent_scope:` kwargs.
2. AR uniqueness on `:code` uses `%i[entity_type scope parent_scope]`.
3. `validate_parent_scope_invariant` rejects orphan-parent rows on Section.
4. `bin/rspec spec/models/typed_eav/` runs without new failures.
5. Single-scope BC preserved: `Section.for_entity(et, scope: "t1")` continues to work.
6. Section implementation is symmetric to Field::Base (same logic shape, only differing in `:code` vs `:name`).
7. `git diff main -- app/models/typed_eav/section.rb` shows the changes.
</verification>
<success_criteria>
- `Section.for_entity` is partition-aware on (entity_type, scope, parent_scope).
- Uniqueness is enforced at both AR validator and DB index layers on `(entity_type, code, scope, parent_scope)`.
- Orphan-parent rows are rejected at section-definition write time.
- Section's logic is intentionally symmetric to Field::Base (mechanical future extraction).
- Single atomic commit.
</success_criteria>
<output>
01-04-SUMMARY.md
</output>

## Plan amendment (R01) — single-commit consolidation is prescribed

**DEVIATION-01-04**: Same shape as DEVIATION-01-03 (the symmetric `Section` change). The QA gate flagged that tasks 1-3 (`Section.for_entity` expansion, uniqueness expansion, `validate_parent_scope_invariant`) shipped as a single coordinated edit rather than three sequential commits. This IS the prescribed execution model: task 4 explicitly directs "Single commit, one file." Tasks 1-3 modify adjacent regions of `app/models/typed_eav/section.rb` and ship as a single atomic commit (commit `9c7e916`). Each task's done criterion was independently verified before commit. Future readers: task numbering is organizational decomposition, not a commit-boundary specification — the entire plan ships as one atomic commit, intentionally symmetric to plan 03's Field::Base consolidation.

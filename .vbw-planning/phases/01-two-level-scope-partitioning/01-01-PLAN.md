---
phase: 1
plan: 01
title: Migration — add parent_scope column and triple-aware indexes
type: execute
wave: 0
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: [database-migrations]
files_modified:
  - db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb
forbidden_commands: []
must_haves:
  truths:
    - "typed_eav_fields.parent_scope column exists, type=string, nullable"
    - "typed_eav_sections.parent_scope column exists, type=string, nullable"
    - "Old indexes idx_te_fields_unique_scoped, idx_te_fields_unique_global, idx_te_sections_unique_scoped, idx_te_sections_unique_global, idx_te_fields_lookup are dropped"
    - "New paired partial unique indexes idx_te_fields_uniq_scoped (cols name,entity_type,scope,parent_scope WHERE scope IS NOT NULL) and idx_te_fields_uniq_global (cols name,entity_type WHERE scope IS NULL) exist"
    - "New paired partial unique indexes idx_te_sections_uniq_scoped (cols entity_type,code,scope,parent_scope WHERE scope IS NOT NULL) and idx_te_sections_uniq_global (cols entity_type,code WHERE scope IS NULL) exist"
    - "idx_te_fields_lookup recreated with columns entity_type,scope,parent_scope,sort_order,name"
    - "New idx_te_sections_lookup created with columns entity_type,scope,parent_scope,sort_order,name (parity with fields)"
    - "Migration uses disable_ddl_transaction! and algorithm: :concurrently for all index DROP/CREATE; uses up/down (not change) for reversibility"
    - "Every remove_index uses if_exists: true; every add_index uses if_not_exists: true (idempotent re-runs)"
    - "All new index names <= 63 bytes (Postgres identifier limit)"
    - "spec/dummy db migrates cleanly: bin/rails db:drop db:create db:migrate from spec/dummy succeeds"
    - "Single commit: db(migration): add parent_scope to typed_eav fields and sections partition tuple"
  artifacts:
    - path: "db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb"
      provides: "Engine migration creating parent_scope column + new partition indexes"
      contains: "disable_ddl_transaction!"
    - path: "db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb"
      provides: "Up method"
      contains: "add_column :typed_eav_fields, :parent_scope"
    - path: "db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb"
      provides: "Concurrent index DDL"
      contains: "algorithm: :concurrently"
  key_links:
    - {from: "db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb", to: "db/migrate/20260330000000_create_typed_eav_tables.rb", via: "establishes phase-1 schema follow-on; mirrors paired-partial-index pattern from initial migration"}
---
<objective>
Add a nullable `parent_scope` (string) column to `typed_eav_fields` and `typed_eav_sections`, drop the five existing scope-partition indexes, and create the new triple-aware paired-partial-unique and lookup indexes for both tables. This is the schema foundation that every other Phase-1 plan depends on.

This is the gem's first follow-on migration after the initial create. It establishes the `disable_ddl_transaction!` + `algorithm: :concurrently` pattern that future production-deployed migrations will follow, since consumer apps may have millions of rows in `typed_eav_fields` / `typed_eav_sections` when they upgrade.

Decision rationale (recorded here for the SUMMARY): include `idx_te_sections_lookup` as a brand-new index for parallelism with `idx_te_fields_lookup` even though Phase 1 success criteria don't strictly require it. RESEARCH §7 flags the asymmetry; adding it now is cheap (one more concurrent CREATE) and avoids a follow-on migration in Phase 2 when section ordering helpers ship.
</objective>
<context>
@.vbw-planning/phases/01-two-level-scope-partitioning/01-CONTEXT.md
@.vbw-planning/phases/01-two-level-scope-partitioning/01-RESEARCH.md
@db/migrate/20260330000000_create_typed_eav_tables.rb

Key facts from research:
- Initial migration is the only one today; this establishes follow-on migration conventions.
- Five existing indexes need DROP + CREATE: idx_te_fields_unique_scoped, idx_te_fields_unique_global, idx_te_sections_unique_scoped, idx_te_sections_unique_global, idx_te_fields_lookup. (Pre-existing simple `t.index :entity_type` on fields stays.)
- Paired-partial pattern: scoped partial covers (cols including scope, parent_scope) WHERE scope IS NOT NULL; global partial covers (cols WITHOUT scope, parent_scope) WHERE scope IS NULL. Reason: the orphan-parent invariant guarantees parent_scope IS NULL whenever scope IS NULL, so adding parent_scope to the global partial would be redundant.
- Index names are renamed from `_unique_` to `_uniq_` to keep all under 30 bytes (well within 63).
</context>
<tasks>
<task type="auto">
  <name>Create migration file with disable_ddl_transaction and add_column for both tables</name>
  <files>
    db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb
  </files>
  <action>
Create the migration with class name `AddParentScopeToTypedEavPartitions`, inheriting `ActiveRecord::Migration[7.1]` (matches existing migration's Rails version). Call `disable_ddl_transaction!` at the top of the class body (required because we will use `algorithm: :concurrently` for index ops; concurrent operations cannot run inside a DDL transaction).

Implement `up` and `down` methods (NOT `change`, because `algorithm: :concurrently` is not auto-reversible).

In `up`, first:
  add_column :typed_eav_fields, :parent_scope, :string
  add_column :typed_eav_sections, :parent_scope, :string

`add_column` with a nullable string is a Postgres catalog-only change — instantaneous, no table rewrite — and is safe even though we're outside a transaction.

In `down`, the column removal will run last (after dropping new indexes and restoring old ones); see later tasks. For now, just stub `down` with a comment placeholder.
  </action>
  <verify>
Run `bin/rails db:migrate` from spec/dummy. The new migration runs without error. `psql -c "\d typed_eav_fields"` (or AR introspection) shows the `parent_scope` column on both tables. Run `bin/rails db:rollback` and the rollback path still raises (placeholder) — to be filled in by later tasks.
  </verify>
  <done>
Migration file exists at the correct path. Class name, parent class, `disable_ddl_transaction!`, and the two `add_column` calls are in place. `up` is partial; `down` is a placeholder.
  </done>
</task>
<task type="auto">
  <name>Drop the five existing scope-partition indexes (idempotent)</name>
  <files>
    db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb
  </files>
  <action>
In `up`, after the `add_column` calls, drop each of the five existing indexes using `remove_index` with `if_exists: true, algorithm: :concurrently`. The indexes to drop:

  remove_index :typed_eav_fields,   name: :idx_te_fields_unique_scoped,   if_exists: true, algorithm: :concurrently
  remove_index :typed_eav_fields,   name: :idx_te_fields_unique_global,   if_exists: true, algorithm: :concurrently
  remove_index :typed_eav_fields,   name: :idx_te_fields_lookup,          if_exists: true, algorithm: :concurrently
  remove_index :typed_eav_sections, name: :idx_te_sections_unique_scoped, if_exists: true, algorithm: :concurrently
  remove_index :typed_eav_sections, name: :idx_te_sections_unique_global, if_exists: true, algorithm: :concurrently

`if_exists: true` keeps the migration idempotent on re-run (e.g., a partial failure during a long index drop on production).
  </action>
  <verify>
After running migration, query `pg_indexes` (or use AR's `connection.indexes(:typed_eav_fields)`): none of the five named indexes appear. The simple `t.index :entity_type` (unnamed) on `typed_eav_fields` still exists.
  </verify>
  <done>
All five `remove_index` calls present in `up`, each with `if_exists: true` and `algorithm: :concurrently`.
  </done>
</task>
<task type="auto">
  <name>Create new triple-aware paired-partial unique indexes and lookup indexes</name>
  <files>
    db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb
  </files>
  <action>
In `up`, after the index drops, create the new indexes with `algorithm: :concurrently, if_not_exists: true`. Six total: four paired-partial unique + one fields lookup + one new sections lookup.

  add_index :typed_eav_fields,
            %i[name entity_type scope parent_scope],
            unique: true,
            where: "scope IS NOT NULL",
            name: :idx_te_fields_uniq_scoped,
            algorithm: :concurrently,
            if_not_exists: true

  add_index :typed_eav_fields,
            %i[name entity_type],
            unique: true,
            where: "scope IS NULL",
            name: :idx_te_fields_uniq_global,
            algorithm: :concurrently,
            if_not_exists: true

  add_index :typed_eav_sections,
            %i[entity_type code scope parent_scope],
            unique: true,
            where: "scope IS NOT NULL",
            name: :idx_te_sections_uniq_scoped,
            algorithm: :concurrently,
            if_not_exists: true

  add_index :typed_eav_sections,
            %i[entity_type code],
            unique: true,
            where: "scope IS NULL",
            name: :idx_te_sections_uniq_global,
            algorithm: :concurrently,
            if_not_exists: true

  add_index :typed_eav_fields,
            %i[entity_type scope parent_scope sort_order name],
            name: :idx_te_fields_lookup,
            algorithm: :concurrently,
            if_not_exists: true

  add_index :typed_eav_sections,
            %i[entity_type scope parent_scope sort_order name],
            name: :idx_te_sections_lookup,
            algorithm: :concurrently,
            if_not_exists: true

The pattern matches the original migration's paired-partial design: scoped partial includes scope+parent_scope; global partial omits both because the orphan-parent invariant (scope IS NULL implies parent_scope IS NULL) makes it redundant. This means duplicate global definitions across parent_scope values cannot exist by construction (writes that try are rejected by the model-level invariant validator added in plan 03 and 04).
  </action>
  <verify>
After running migration, `connection.indexes(:typed_eav_fields).map(&:name)` includes `idx_te_fields_uniq_scoped`, `idx_te_fields_uniq_global`, `idx_te_fields_lookup`. Similar for `typed_eav_sections`. Each index's columns and `where` clause match the spec above. Inspect partial-index where clauses: `psql -c "SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'typed_eav_fields'"`.
  </verify>
<done>
All six `add_index` calls present in `up`, each with `algorithm: :concurrently` and `if_not_exists: true`. Index names are within 63-byte limit.
  </done>
</task>
<task type="auto">
  <name>Implement down method (reversal): drop new indexes, restore old indexes, remove column</name>
  <files>
    db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb
  </files>
  <action>
Replace the `down` placeholder with the full reversal. Reverse order: drop the six new indexes first, recreate the five original indexes (matching the original migration verbatim except using `algorithm: :concurrently`), then `remove_column` the parent_scope columns.

  def down
    # Drop new triple-aware indexes
    remove_index :typed_eav_fields,   name: :idx_te_fields_uniq_scoped,   if_exists: true, algorithm: :concurrently
    remove_index :typed_eav_fields,   name: :idx_te_fields_uniq_global,   if_exists: true, algorithm: :concurrently
    remove_index :typed_eav_fields,   name: :idx_te_fields_lookup,        if_exists: true, algorithm: :concurrently
    remove_index :typed_eav_sections, name: :idx_te_sections_uniq_scoped, if_exists: true, algorithm: :concurrently
    remove_index :typed_eav_sections, name: :idx_te_sections_uniq_global, if_exists: true, algorithm: :concurrently
    remove_index :typed_eav_sections, name: :idx_te_sections_lookup,      if_exists: true, algorithm: :concurrently

    # Restore original indexes (verbatim from db/migrate/20260330000000_create_typed_eav_tables.rb)
    add_index :typed_eav_fields, %i[name entity_type scope],
              unique: true, where: "scope IS NOT NULL",
              name: :idx_te_fields_unique_scoped,
              algorithm: :concurrently, if_not_exists: true
    add_index :typed_eav_fields, %i[name entity_type],
              unique: true, where: "scope IS NULL",
              name: :idx_te_fields_unique_global,
              algorithm: :concurrently, if_not_exists: true
    add_index :typed_eav_fields, %i[entity_type scope sort_order name],
              name: :idx_te_fields_lookup,
              algorithm: :concurrently, if_not_exists: true
    add_index :typed_eav_sections, %i[entity_type code scope],
              unique: true, where: "scope IS NOT NULL",
              name: :idx_te_sections_unique_scoped,
              algorithm: :concurrently, if_not_exists: true
    add_index :typed_eav_sections, %i[entity_type code],
              unique: true, where: "scope IS NULL",
              name: :idx_te_sections_unique_global,
              algorithm: :concurrently, if_not_exists: true

    # Drop columns
    remove_column :typed_eav_sections, :parent_scope
    remove_column :typed_eav_fields,   :parent_scope
  end

Down rolls back to the exact pre-migration shape. The five recreated indexes use the original names (`_unique_` not `_uniq_`).
  </action>
  <verify>
Run `bin/rails db:migrate` then `bin/rails db:rollback` from spec/dummy. Both succeed. After rollback: `parent_scope` columns are gone and the five original indexes are present. Re-run `db:migrate` and the migration runs forward cleanly again (idempotent guards work).
  </verify>
  <done>
`down` mirrors `up` precisely (drop new, recreate old, drop column). Two full round-trips of migrate→rollback→migrate succeed without error.
  </done>
</task>
<task type="auto">
  <name>Verify schema and commit</name>
  <files>
    db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb
  </files>
  <action>
From spec/dummy: `bin/rails db:drop db:create db:migrate` to perform a clean migration run. Then run `bin/rspec spec/lib/typed_eav/scoping_spec.rb` to confirm the existing scope suite still passes against the new schema (the column is nullable; nothing reads it yet, so behavior should be unchanged at this point). Existing tests must remain green.

Commit with message: `db(migration): add parent_scope to typed_eav fields and sections partition tuple`. Single commit, single file changed.

Do NOT bump version, push, or update CHANGELOG — those land in plan 07.
  </action>
  <verify>
`git log --oneline -1` shows the commit. `git diff HEAD~1 HEAD --stat` shows exactly one file changed. `bin/rspec spec/lib/typed_eav/scoping_spec.rb` exit code 0. `bin/rails runner "p ActiveRecord::Base.connection.column_exists?(:typed_eav_fields, :parent_scope)"` from spec/dummy prints `true`.
  </verify>
  <done>
Migration committed, schema is in place, existing scoping spec unchanged-and-passing, no version bump or CHANGELOG edit.
  </done>
</task>
</tasks>
<verification>
1. `bin/rspec spec/lib/typed_eav/scoping_spec.rb` passes (existing behavior preserved).
2. From spec/dummy: `bin/rails runner "puts ActiveRecord::Base.connection.indexes(:typed_eav_fields).map(&:name).sort"` lists `idx_te_fields_uniq_scoped`, `idx_te_fields_uniq_global`, `idx_te_fields_lookup` (and the unchanged `index_typed_eav_fields_on_entity_type` from the original `t.index :entity_type`).
3. Same for `typed_eav_sections` lists `idx_te_sections_uniq_scoped`, `idx_te_sections_uniq_global`, `idx_te_sections_lookup`, plus the unchanged `idx_te_sections_entity_active`.
4. Both columns exist: `connection.column_exists?(:typed_eav_fields, :parent_scope)` → true; same for `typed_eav_sections`.
5. Migrate→rollback→migrate succeeds end-to-end without error.
6. `git diff main -- db/migrate/` shows exactly one new file.
</verification>
<success_criteria>
- `parent_scope` column exists on `typed_eav_fields` and `typed_eav_sections`, both nullable strings.
- All 5 original scope indexes have been dropped and replaced with their triple-aware equivalents (or kept-name in the case of `idx_te_fields_lookup`).
- New `idx_te_sections_lookup` exists for parity with `idx_te_fields_lookup`.
- Migration is reversible: `down` restores the original schema exactly.
- Migration uses `disable_ddl_transaction!` + `algorithm: :concurrently` + idempotent guards (`if_exists` / `if_not_exists`).
- All existing specs pass against the new schema (no behavior change at this layer).
- Single atomic commit using project commit-format.
</success_criteria>
<output>
01-01-SUMMARY.md
</output>

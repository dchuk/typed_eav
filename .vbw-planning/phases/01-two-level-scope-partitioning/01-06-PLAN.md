---
phase: 1
plan: 06
title: Spec coverage — scoping, regressions, field/value, factories
type: execute
wave: 3
depends_on: ["01-05"]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: [tdd-cycle]
files_modified:
  - spec/lib/typed_eav/scoping_spec.rb
  - spec/regressions/review_round_2_scope_leak_spec.rb
  - spec/regressions/review_round_3_collision_spec.rb
  - spec/regressions/review_round_4_parent_scope_spec.rb
  - spec/models/typed_eav/value_spec.rb
  - spec/models/typed_eav/field_spec.rb
  - spec/factories/typed_eav.rb
forbidden_commands: []
must_haves:
  truths:
    - "spec/lib/typed_eav/scoping_spec.rb assertions updated to expect [scope, parent_scope] tuples from current_scope, with_scope, and DEFAULT_SCOPE_RESOLVER"
    - "scoping_spec covers new with_scope([s, ps]) tuple form; scalar with_scope(v) BC still asserted"
    - "scoping_spec covers typed_eav_definitions(parent_scope: ...) kwarg with parallel coverage to the existing scope: kwarg block"
    - "scoping_spec covers Section.for_entity with parent_scope: kwarg (parallel to today's scope: coverage)"
    - "Custom resolver returning a bare scalar raises ArgumentError under the new contract — covered by a regression assertion"
    - "review_round_2_scope_leak_spec.rb extended with parent_scope-axis leak coverage that mirrors the existing scope-axis assertions: (a) Product (un-scoped host) ignores ambient parent_scope just as it ignores ambient scope; (b) Contact-on-a-parent_scope-bearing-host equivalent honors ambient parent_scope; (c) `unscoped { }` block must NOT leak parent_scope filters; (d) `scope: nil, parent_scope: nil` kwarg means pure-global"
    - "review_round_3_collision_spec.rb extended with three-way collision precedence coverage (global / scope-only / full-triple) — the comparator chosen in plan 05's `definitions_by_name` must be exercised here; multimap branch under `unscoped { }` must collapse across (scope, parent_scope) combinations for a given name"
    - "New file spec/regressions/review_round_4_parent_scope_spec.rb exists, follows the round_2/round_3 file shape, and covers ONLY cross-cutting scenarios that don't fit cleanly into round_2 or round_3: (a) orphan-parent rejection at Field level (parent_scope set + scope nil), (b) symmetric orphan-parent rejection at Section level, (c) cross-axis Value rejection (Value attached to a Field whose parent_scope doesn't match entity's typed_eav_parent_scope, including the host-without-parent_scope_method case)"
    - "spec/models/typed_eav/field_spec.rb gains tests for the new validate_parent_scope_invariant and the broadened uniqueness key (entity_type, scope, parent_scope)"
    - "spec/models/typed_eav/value_spec.rb gains tests for the parent_scope axis on validate_field_scope_matches_entity (extending the existing :unscoped 'cross scope' block)"
    - "spec/factories/typed_eav.rb factories accept parent_scope: as a passable attribute (no default — tests opt in); a project factory or equivalent exists for the parent_scope_method test"
    - "Full suite (bin/rspec) is green on a clean db: 0 failures, 0 errors"
    - "Single commit: test(scope): cover parent_scope axis across resolver, query path, regressions, and unit specs"
  artifacts:
    - path: "spec/regressions/review_round_2_scope_leak_spec.rb"
      provides: "Existing scope-leak regressions extended with parent_scope-axis mirrors"
      contains: "parent_scope"
    - path: "spec/regressions/review_round_3_collision_spec.rb"
      provides: "Existing collision regressions extended with three-way precedence and parent_scope multimap collapse"
      contains: "parent_scope"
    - path: "spec/regressions/review_round_4_parent_scope_spec.rb"
      provides: "Round-4 regression file — cross-cutting parent_scope scenarios only (orphan rejection, cross-axis Value rejection)"
      contains: "RSpec.describe \"Round-4 review: parent_scope cross-cutting\""
    - path: "spec/lib/typed_eav/scoping_spec.rb"
      provides: "Tuple-aware resolver chain coverage"
      contains: "with_scope([\"t1\", \"w1\"])"
    - path: "spec/factories/typed_eav.rb"
      provides: "Project factory or equivalent for parent_scope_method tests"
      contains: "factory :project"
  key_links:
    - {from: "spec/regressions/review_round_2_scope_leak_spec.rb", to: "spec/regressions/review_round_3_collision_spec.rb", via: "extended in parallel; round_2 owns leak axis (now both scope and parent_scope), round_3 owns collision axis (now both)"}
    - {from: "spec/regressions/review_round_4_parent_scope_spec.rb", to: "spec/regressions/review_round_3_collision_spec.rb", via: "follows the same describe-bug structure; round-4 owns cross-cutting parent_scope scenarios that don't fit round_2 or round_3"}
    - {from: "spec/lib/typed_eav/scoping_spec.rb", to: "lib/typed_eav.rb", via: "asserts the new tuple contract on TypedEAV.current_scope, with_scope, normalize_scope (plan 02)"}
---
<objective>
Bring the test suite back to fully green by updating the existing scoping assertions to the new tuple contract and adding parallel coverage for the parent_scope axis. After this plan, the full `bin/rspec` run from spec/dummy reports 0 failures.

Per Lead's discretion call (revised): all three round files participate. `review_round_2_scope_leak_spec.rb` and `review_round_3_collision_spec.rb` are EXTENDED in place with parent_scope-axis coverage that mirrors their existing scope-axis assertions — round_2 owns the leak axis (now both `scope` and `parent_scope` as first-class regressions), round_3 owns the collision axis (now both two-way and three-way precedence). A new file `spec/regressions/review_round_4_parent_scope_spec.rb` covers ONLY cross-cutting scenarios that don't naturally belong in either of the existing rounds — orphan-parent rejection at Field/Section level, and cross-axis Value rejection.

Rationale for this split: keeping the parent_scope leak coverage in round_2 (rather than siphoning it into round_4) means each round file remains a complete regression record for the failure mode it owns. A future maintainer reading round_2 to understand "what does the un-scoped-host short-circuit guarantee?" will see both axes guarded together — they will not have to cross-reference round_4 to discover that the parent_scope leak is also covered. Symmetrically, round_3's three-way precedence belongs alongside round_3's existing two-way precedence for the same reason. Round_4 then becomes a focused, narrow regression file for the genuinely-new failure modes that don't have a round_2 or round_3 ancestor to extend (orphan-parent invariant; Value-side cross-axis guard).

This plan does NOT modify production code. If a spec genuinely cannot pass without a behavioral fix, that's a discovered issue — pause, surface to the Lead/orchestrator, and fix forward via a follow-up plan rather than touching production code from this spec plan.
</objective>
<context>
@.vbw-planning/phases/01-two-level-scope-partitioning/01-CONTEXT.md
@.vbw-planning/phases/01-two-level-scope-partitioning/01-RESEARCH.md
@spec/lib/typed_eav/scoping_spec.rb
@spec/regressions/review_round_2_scope_leak_spec.rb
@spec/regressions/review_round_3_collision_spec.rb
@spec/factories/typed_eav.rb
@spec/dummy/app/models/test_models.rb

Coverage gaps to close (from RESEARCH §8, revised by Lead post-research):
- scoping_spec: every `expect(TypedEAV.current_scope).to eq("t1")` becomes `eq(["t1", nil])`. Same for resolver chain and acts_as_tenant bridge tests.
- New describe blocks for: with_scope([s, ps]) tuple form; typed_eav_definitions parent_scope: kwarg behavior; Section.for_entity parent_scope: kwarg; resolve_scope ArgumentError on bare-scalar resolver returns.
- value_spec: extend the :unscoped block's "cross-scope write" assertion with a parallel "cross-(scope, parent_scope) write" assertion.
- field_spec: tests for orphan-parent rejection and triple-uniqueness.
- review_round_2: extend existing Product/Contact describe blocks with parent_scope-axis mirrors of every existing scope-axis assertion.
- review_round_3: extend Bug 1 (multimap under unscoped) to OR-collapse across (scope, parent_scope) combinations; extend Bug 2 (collision precedence) from two-way (global vs scoped) to three-way (global vs scope-only vs full-triple).
- New round_4 regression file covering ONLY cross-cutting scenarios: orphan-parent rejection (Field + Section), cross-axis Value rejection.
- Factories: a `project` factory matching the spec/dummy Project model from plan 05.
</context>
<tasks>
<task type="auto">
  <name>Update existing scoping_spec assertions to the tuple contract; add tuple-form with_scope and parent_scope kwarg coverage</name>
  <files>
    spec/lib/typed_eav/scoping_spec.rb
  </files>
  <action>
Update every assertion in spec/lib/typed_eav/scoping_spec.rb that expects a scalar from `current_scope`, `with_scope`, the resolver chain, or the acts_as_tenant bridge. Concrete pattern: `expect(TypedEAV.current_scope).to eq("t1")` → `expect(TypedEAV.current_scope).to eq(["t1", nil])`. Same for "from_block", "from_resolver", "42", "7", and "99" assertions.

Be careful: the assertions on `expect(TypedEAV.current_scope).to be_nil` STAY unchanged — `current_scope` returns nil (not `[nil, nil]`) outside any block when nothing resolves AND inside `unscoped { }`. The tuple shape is `[scope, parent_scope]` OR nil.

Add new describe blocks (place near the related existing block):

  describe ".with_scope (tuple form)" do
    it "accepts [scope, parent_scope] and exposes both via current_scope" do
      TypedEAV.with_scope(["t1", "w1"]) do
        expect(TypedEAV.current_scope).to eq(["t1", "w1"])
      end
    end

    it "scalar form remains backwards-compatible (parent_scope=nil)" do
      TypedEAV.with_scope("t1") do
        expect(TypedEAV.current_scope).to eq(["t1", nil])
      end
    end

    it "normalizes AR-record halves to id.to_s within a tuple" do
      tenant = Struct.new(:id).new(7)
      workspace = Struct.new(:id).new(11)
      TypedEAV.with_scope([tenant, workspace]) do
        expect(TypedEAV.current_scope).to eq(["7", "11"])
      end
    end

    it "preserves the orphan-parent shape verbatim (validators run elsewhere)" do
      TypedEAV.with_scope([nil, "w1"]) do
        expect(TypedEAV.current_scope).to eq([nil, "w1"])
      end
    end
  end

  describe "resolver-callable contract violation" do
    it "raises ArgumentError when a configured resolver returns a bare scalar" do
      TypedEAV.config.scope_resolver = -> { "t1" } # v0.1.x style
      expect do
        Contact.where_typed_eav({ name: "age", op: :eq, value: 30 })
      end.to raise_error(ArgumentError, /must return a 2-element \[scope, parent_scope\] Array/)
    end
  end

  describe "typed_eav_definitions parent_scope kwarg" do
    let!(:full_triple) { create(:integer_field, name: "x", entity_type: "Project", scope: "t1", parent_scope: "w1") }
    let!(:scope_only)  { create(:integer_field, name: "x", entity_type: "Project", scope: "t1") }
    let!(:global)      { create(:integer_field, name: "x", entity_type: "Project") }

    it "filters to (full triple + scope-only + global) for matching (scope, parent_scope)" do
      fields = Project.typed_eav_definitions(scope: "t1", parent_scope: "w1")
      expect(fields).to contain_exactly(full_triple, scope_only, global)
    end

    it "filters to (scope-only + global) when parent_scope is nil" do
      fields = Project.typed_eav_definitions(scope: "t1", parent_scope: nil)
      expect(fields).to contain_exactly(scope_only, global)
    end
  end

  describe "Section#for_entity with parent_scope" do
    let!(:full)   { TypedEAV::Section.create!(name: "F", code: "f", entity_type: "Project", scope: "t1", parent_scope: "w1") }
    let!(:scope_only) { TypedEAV::Section.create!(name: "S", code: "s", entity_type: "Project", scope: "t1") }
    let!(:global) { TypedEAV::Section.create!(name: "G", code: "g", entity_type: "Project") }

    it "returns full-triple + scope-only + global for matching axes" do
      result = TypedEAV::Section.for_entity("Project", scope: "t1", parent_scope: "w1")
      expect(result).to contain_exactly(full, scope_only, global)
    end

    it "returns only globals when scope and parent_scope omitted" do
      expect(TypedEAV::Section.for_entity("Project")).to contain_exactly(global)
    end
  end

These four new describe blocks plus the assertion-shape updates close the scoping_spec coverage requirements from RESEARCH §8.
  </action>
  <verify>
`bin/rspec spec/lib/typed_eav/scoping_spec.rb` — all examples pass. Inspect the file: every existing `eq("...")` against `current_scope` is updated to `eq([...])`; new describe blocks are present.
  </verify>
  <done>
Existing assertions updated to tuple shape; four new describe blocks added; spec file is green.
  </done>
</task>
<task type="auto">
  <name>Update value_spec, field_spec, and add :project factory</name>
  <files>
    spec/models/typed_eav/value_spec.rb
    spec/models/typed_eav/field_spec.rb
    spec/factories/typed_eav.rb
  </files>
  <action>
In spec/factories/typed_eav.rb, add a `:project` factory matching the spec/dummy Project model added in plan 05:

  factory :project do
    sequence(:name) { |n| "Project #{n}" }
    tenant_id { nil }
    workspace_id { nil }
  end

Confirm none of the existing field factories (`:text_field`, `:integer_field`, etc.) auto-set `parent_scope`. Adding a default would make `parent_scope: nil` an unnecessary explicit override in every existing test. Tests that need a parent_scope value pass it explicitly: `create(:integer_field, scope: "t1", parent_scope: "w1")`. The plan-03 `Field::Base.parent_scope` column defaults to nil at the DB level, so `nil` is the natural fixture default.

In spec/models/typed_eav/value_spec.rb, find the existing `:unscoped` metadata block at line 96 ("REVIEW: nested typed-value must not attach across scope") and add a sibling block for the parent_scope axis:

  describe "REVIEW: nested typed-value must not attach across parent_scope", :unscoped do
    let!(:full_triple_field) { create(:integer_field, entity_type: "Project", scope: "t1", parent_scope: "w1") }

    it "rejects when entity is in a different parent_scope" do
      project = Project.create!(name: "p", tenant_id: "t1", workspace_id: "w2")
      value = TypedEAV::Value.new(entity: project, field: full_triple_field, value: 1)
      expect(value).not_to be_valid
      expect(value.errors.added?(:field, :invalid)).to be true
    end

    it "rejects when entity host has no parent_scope_method (Contact -> full-triple field)" do
      contact = Contact.create!(name: "c", tenant_id: "t1")
      value = TypedEAV::Value.new(entity: contact, field: full_triple_field, value: 1)
      expect(value).not_to be_valid
    end

    it "accepts when both parent_scopes match" do
      project = Project.create!(name: "p", tenant_id: "t1", workspace_id: "w1")
      value = TypedEAV::Value.new(entity: project, field: full_triple_field, value: 1)
      expect(value).to be_valid
    end
  end

(If `:unscoped` metadata isn't actually used by other suites for this purpose, drop the metadata tag — match the existing block's metadata exactly.)

In spec/models/typed_eav/field_spec.rb, find the existing "enforces name uniqueness per entity_type and scope" test (around line 19-30 per RESEARCH §8) and:

1. Update the existing test if necessary to include `parent_scope: nil` explicitly (no behavior change — fixtures default to nil).
2. Add a new context block:

  context "with parent_scope partitioning" do
    let!(:scoped_only) { create(:integer_field, name: "x", entity_type: "Project", scope: "t1") }

    it "allows a same-name field with a different parent_scope under the same scope" do
      f = build(:integer_field, name: "x", entity_type: "Project", scope: "t1", parent_scope: "w1")
      expect(f).to be_valid
    end

    it "rejects a duplicate within the same (entity_type, scope, parent_scope) tuple" do
      create(:integer_field, name: "y", entity_type: "Project", scope: "t1", parent_scope: "w1")
      dup = build(:integer_field, name: "y", entity_type: "Project", scope: "t1", parent_scope: "w1")
      expect(dup).not_to be_valid
      expect(dup.errors[:name]).to be_present
    end

    it "rejects an orphan-parent (parent_scope set, scope nil)" do
      f = build(:integer_field, name: "z", entity_type: "Project", scope: nil, parent_scope: "w1")
      expect(f).not_to be_valid
      expect(f.errors[:parent_scope]).to be_present
    end

    it "accepts (scope nil, parent_scope nil) — pure global" do
      f = build(:integer_field, name: "g1", entity_type: "Project", scope: nil, parent_scope: nil)
      expect(f).to be_valid
    end

    it "accepts (scope set, parent_scope nil) — scope-only (existing shape)" do
      f = build(:integer_field, name: "s1", entity_type: "Project", scope: "t1", parent_scope: nil)
      expect(f).to be_valid
    end
  end

These tests close the field-level coverage gap (orphan-parent rejection + triple uniqueness).
  </action>
  <verify>
`bin/rspec spec/models/typed_eav/field_spec.rb spec/models/typed_eav/value_spec.rb` — all examples pass. `bin/rails runner "p FactoryBot.build(:project).attributes" 2>&1` from spec/dummy prints attributes including `tenant_id: nil, workspace_id: nil`. `bin/rails runner "p FactoryBot.build(:integer_field).attributes['parent_scope']"` prints `nil`. `git diff --stat spec/factories/typed_eav.rb` shows the file was modified.
  </verify>
  <done>
Value spec gains a parent_scope cross-axis describe block; Field spec gains a parent_scope partitioning context with five examples; :project factory exists; no field factory has a `parent_scope` default; all three files green.
  </done>
</task>
<task type="auto">
  <name>Extend review_round_2 and review_round_3 with parent_scope coverage</name>
  <files>
    spec/regressions/review_round_2_scope_leak_spec.rb
    spec/regressions/review_round_3_collision_spec.rb
  </files>
  <action>
Both files are extended in place — the work pattern is the same for both: mirror each existing scope-axis assertion with a parent_scope-axis sibling, using the spec/dummy Project model (which has both `tenant_id` and `workspace_id`) as the parent_scope-bearing host. Do NOT delete or restructure existing assertions; only add new describe blocks (or new `it` blocks within existing describe blocks where the mirror is small enough to read in line).

== A. spec/regressions/review_round_2_scope_leak_spec.rb ==

Add a new describe block "Project (no parent_scope_method: declared, scope_method: only) — ambient parent_scope must not leak" parallel to the existing "Product (no scope_method: declared)" block. Project is a partial-opt-in model: it declares `scope_method: :tenant_id` but NOT `parent_scope_method:`. The short-circuit guarantee mirrored from round_2's existing scope coverage: a model that did not opt into the parent_scope axis must not see parent_scope filtering on its definitions, even when an ambient parent_scope is on the stack.

Concrete shape (5 it blocks, mirroring the existing 6 in the Product block — drop the configured-resolver one since it duplicates the with_scope path mechanically):

  describe "Project (scope_method: only, no parent_scope_method:) — ambient parent_scope must not leak" do
    let!(:project_full_triple) do
      create(:integer_field, name: "weight_full", entity_type: "Project", scope: "tenant_a", parent_scope: "w1")
    end
    let!(:project_scope_only) do
      create(:integer_field, name: "weight_scoped", entity_type: "Project", scope: "tenant_a")
    end
    let!(:project_global) do
      create(:integer_field, name: "weight", entity_type: "Project", scope: nil)
    end

    it "with_scope(['tenant_a', 'w1']) on a host without parent_scope_method: ignores parent_scope, returns scope-only + global" do
      TypedEAV.with_scope(["tenant_a", "w1"]) do
        # The host opted into scope only, so parent_scope axis stays inert: parent_scope
        # filter is dropped, scope filter still applies. Result: scope_only + global, no full_triple.
        fields = Project.typed_eav_definitions
        expect(fields).to contain_exactly(project_scope_only, project_global)
        expect(fields).not_to include(project_full_triple)
      end
    end

    it "explicit parent_scope: kwarg overrides the host short-circuit (admin/test path)" do
      fields = Project.typed_eav_definitions(scope: "tenant_a", parent_scope: "w1")
      expect(fields).to include(project_full_triple, project_scope_only, project_global)
    end

    it "explicit parent_scope: nil keeps full-triple fields out (parallel to scope: nil)" do
      TypedEAV.with_scope(["tenant_a", "w1"]) do
        fields = Project.typed_eav_definitions(parent_scope: nil)
        expect(fields).to contain_exactly(project_scope_only, project_global)
      end
    end

    it "inside TypedEAV.unscoped { } returns fields across ALL (scope, parent_scope) combinations" do
      TypedEAV.unscoped do
        fields = Project.typed_eav_definitions
        expect(fields).to include(project_full_triple, project_scope_only, project_global)
      end
    end

    it "does not raise even when require_scope is true (the host did opt into scope, but require_scope is keyed on the resolver chain succeeding for scope; parent_scope absence is non-fatal)" do
      TypedEAV.with_scope("tenant_a") do
        expect { Project.typed_eav_definitions }.not_to raise_error
      end
    end
  end

ALSO extend the existing "Product (no scope_method: declared)" block by adding a SINGLE additional it block at the bottom that pins the parent_scope short-circuit invariant for the doubly-un-scoped case:

  it "ignores ambient parent_scope too: with_scope(['tenant_a', 'w1']) still returns globals only" do
    TypedEAV.with_scope(["tenant_a", "w1"]) do
      fields = Product.typed_eav_definitions
      expect(fields).to contain_exactly(product_global)
    end
  end

ALSO extend the existing "Contact (scope_method: :tenant_id declared)" block with a single it block confirming Contact still behaves as before when only a scalar (not a tuple) is on the stack — defensive regression to lock the BC commitment from CONTEXT.md:

  it "with_scope('tenant_a') (scalar BC form) on Contact: returns scoped + global, parent_scope absent" do
    TypedEAV.with_scope("tenant_a") do
      fields = Contact.typed_eav_definitions
      expect(fields).to include(contact_scoped_a, contact_global)
      expect(fields).not_to include(contact_scoped_b)
    end
  end

The Project describe block requires the `:project` factory created in the previous task and assumes spec/dummy/app/models/test_models.rb has Project declaring `has_typed_eav scope_method: :tenant_id` (no parent_scope_method:) — that comes from plan 05.

== B. spec/regressions/review_round_3_collision_spec.rb ==

Two extensions, both mirroring the existing structure:

(B1) Extend "Bug 1: TypedEAV.unscoped + where_typed_eav across tenants" with a parallel describe block "Bug 1 mirror: TypedEAV.unscoped + where_typed_eav across (tenant, workspace) combinations". Use Project (parent_scope-bearing host). Three (tenant, workspace) partitions, each with its own status field. Mirror the existing three it blocks: cross-partition match under unscoped; AND across multiple filters under unscoped; ArgumentError for unknown names under unscoped. Specific shape:

  describe "Bug 1 mirror: TypedEAV.unscoped + where_typed_eav across (tenant, workspace)" do
    let!(:status_t1_w1) { create(:text_field, name: "status", entity_type: "Project", scope: "t1", parent_scope: "w1") }
    let!(:status_t1_w2) { create(:text_field, name: "status", entity_type: "Project", scope: "t1", parent_scope: "w2") }
    let!(:status_t2_w1) { create(:text_field, name: "status", entity_type: "Project", scope: "t2", parent_scope: "w1") }

    let!(:p_t1_w1) do
      create(:project, tenant_id: "t1", workspace_id: "w1").tap { |p| TypedEAV::Value.create!(entity: p, field: status_t1_w1, value: "active") }
    end
    let!(:p_t1_w2) do
      create(:project, tenant_id: "t1", workspace_id: "w2").tap { |p| TypedEAV::Value.create!(entity: p, field: status_t1_w2, value: "active") }
    end
    let!(:p_t2_w1) do
      create(:project, tenant_id: "t2", workspace_id: "w1").tap { |p| TypedEAV::Value.create!(entity: p, field: status_t2_w1, value: "active") }
    end
    let!(:p_t1_w1_inactive) do
      create(:project, tenant_id: "t1", workspace_id: "w1").tap { |p| TypedEAV::Value.create!(entity: p, field: status_t1_w1, value: "inactive") }
    end

    it "OR-collapses matches across all (scope, parent_scope) combinations under unscoped" do
      results = TypedEAV.unscoped do
        Project.where_typed_eav({ name: "status", op: :eq, value: "active" })
      end
      expect(results).to include(p_t1_w1, p_t1_w2, p_t2_w1)
      expect(results).not_to include(p_t1_w1_inactive)
    end

    it "ANDs across multiple filters under unscoped (per-filter OR over name across (scope, parent_scope), AND across filters)" do
      age_t1_w1 = create(:integer_field, name: "age", entity_type: "Project", scope: "t1", parent_scope: "w1")
      age_t1_w2 = create(:integer_field, name: "age", entity_type: "Project", scope: "t1", parent_scope: "w2")
      age_t2_w1 = create(:integer_field, name: "age", entity_type: "Project", scope: "t2", parent_scope: "w1")
      TypedEAV::Value.create!(entity: p_t1_w1, field: age_t1_w1, value: 30)            # match
      TypedEAV::Value.create!(entity: p_t1_w2, field: age_t1_w2, value: 18)            # status match, age fails
      TypedEAV::Value.create!(entity: p_t2_w1, field: age_t2_w1, value: 40)            # match
      TypedEAV::Value.create!(entity: p_t1_w1_inactive, field: age_t1_w1, value: 50)   # age match, status fails

      results = TypedEAV.unscoped do
        Project.where_typed_eav(
          { name: "age", op: :gt, value: 21 },
          { name: "status", op: :eq, value: "active" },
        )
      end
      expect(results).to include(p_t1_w1, p_t2_w1)
      expect(results).not_to include(p_t1_w2)
      expect(results).not_to include(p_t1_w1_inactive)
    end

    it "still raises ArgumentError for unknown field names under unscoped (parent_scope axis)" do
      expect do
        TypedEAV.unscoped do
          Project.where_typed_eav({ name: "nonexistent_typo", op: :eq, value: "x" })
        end
      end.to raise_error(ArgumentError, /Unknown typed field 'nonexistent_typo'/)
    end
  end

(B2) Extend "Bug 2: global+scoped name collision on a scoped record" with a parallel describe block "Bug 2 mirror: three-way collision (global / scope-only / full-triple)". This is the heart of the round_3 extension: today's two-way precedence (scoped > global) becomes three-way (full-triple > scope-only > global). The comparator chosen by Lead in plan 05's update to `definitions_by_name` must be exercised here. Concrete shape (4 it blocks mirroring round_3's existing 4):

  describe "Bug 2 mirror: three-way collision (global / scope-only / full-triple)" do
    let!(:status_global)      { create(:text_field, name: "status", entity_type: "Project", scope: nil) }
    let!(:status_scope_only)  { create(:text_field, name: "status", entity_type: "Project", scope: "tenant_a") }
    let!(:status_full_triple) { create(:text_field, name: "status", entity_type: "Project", scope: "tenant_a", parent_scope: "w1") }
    let(:project) { create(:project, tenant_id: "tenant_a", workspace_id: "w1") }

    it "initialize_typed_values builds exactly ONE value row (full-triple wins)" do
      TypedEAV.with_scope(["tenant_a", "w1"]) do
        project.initialize_typed_values
      end
      status_rows = project.typed_values.select { |tv| tv.field.name == "status" }
      expect(status_rows.size).to eq(1)
      expect(status_rows.first.field_id).to eq(status_full_triple.id)
    end

    it "typed_eav_value('status') returns the full-triple value when all three definitions exist" do
      stray_global = TypedEAV::Value.new
      stray_global.entity = project
      stray_global.field = status_global
      stray_global.value = "global-stray"
      stray_global.save(validate: false)

      stray_scope_only = TypedEAV::Value.new
      stray_scope_only.entity = project
      stray_scope_only.field = status_scope_only
      stray_scope_only.value = "scope-only-stray"
      stray_scope_only.save(validate: false)

      TypedEAV::Value.create!(entity: project, field: status_full_triple, value: "full-triple-wins")

      project.reload
      expect(project.typed_eav_value("status")).to eq("full-triple-wins")
    end

    it "typed_eav_hash prefers the full-triple row when all three exist on the same record" do
      TypedEAV::Value.create!(entity: project, field: status_full_triple, value: "full-triple-wins")
      stray = TypedEAV::Value.new
      stray.entity = project
      stray.field = status_scope_only
      stray.value = "scope-only-stray"
      stray.save(validate: false)
      project.reload
      expect(project.typed_eav_hash["status"]).to eq("full-triple-wins")
    end

    it "scope-only wins over global when no full-triple exists (degenerate case matches today's two-way precedence)" do
      status_full_triple.destroy
      TypedEAV.with_scope(["tenant_a", "w1"]) do
        project.initialize_typed_values
      end
      status_rows = project.typed_values.select { |tv| tv.field.name == "status" }
      expect(status_rows.size).to eq(1)
      expect(status_rows.first.field_id).to eq(status_scope_only.id)
    end
  end

Both extensions assume Project is wired in spec/dummy from plan 05 with `scope_method: :tenant_id, parent_scope_method: :workspace_id`.
  </action>
  <verify>
`bin/rspec spec/regressions/review_round_2_scope_leak_spec.rb` — green, includes the new "Project (scope_method: only ...)" describe block plus the two added it blocks in existing Product/Contact blocks. `bin/rspec spec/regressions/review_round_3_collision_spec.rb` — green, includes the two new "Bug 1 mirror" and "Bug 2 mirror" describe blocks. `git diff --stat spec/regressions/review_round_2_scope_leak_spec.rb spec/regressions/review_round_3_collision_spec.rb` shows both files modified with substantial line additions (each file should grow by ~60-100 lines).
  </verify>
  <done>
review_round_2 owns parent_scope leak coverage as a first-class regression alongside its existing scope coverage; review_round_3 owns three-way precedence and parent_scope multimap collapse alongside its existing two-way precedence; both files green; existing assertions untouched.
  </done>
</task>
<task type="auto">
  <name>Create spec/regressions/review_round_4_parent_scope_spec.rb (cross-cutting scenarios only)</name>
  <files>
    spec/regressions/review_round_4_parent_scope_spec.rb
  </files>
  <action>
Round_4 is now narrowly scoped to cross-cutting parent_scope scenarios that don't naturally belong in round_2 (leak axis) or round_3 (collision axis). With round_2 and round_3 owning their parent_scope mirrors, round_4 covers ONLY:
  - Scenario A — Orphan-parent rejection at Field level (parent_scope set, scope nil): the Field-side invariant doesn't fit round_2 (not about leak) or round_3 (not about collision).
  - Scenario B — Orphan-parent rejection at Section level (symmetric inline-duplicated guard): same rationale.
  - Scenario C — Cross-(scope, parent_scope) Value rejection: this is a Value-side guard that lives at a different layer than the Field-level collision/leak coverage in round_2/round_3.

Create the new file modeled on round_3_collision_spec.rb's overall shape (frozen_string_literal, before/after blocks, top-level describe). Skeleton:

  # frozen_string_literal: true

  require "spec_helper"

  # Round-4 review: cross-cutting parent_scope scenarios for the two-level
  # scope partition introduced in Phase 1.
  #
  # Round_2 owns the leak axis (extended in this phase to cover both scope
  # and parent_scope leaks). Round_3 owns the collision axis (extended in
  # this phase to cover three-way precedence). This file owns the genuinely-
  # new failure modes that don't have a round_2 or round_3 ancestor:
  #
  # Scenario A — Orphan-parent rejection at Field level: a Field row with
  #   parent_scope set and scope nil is invalid. The model-level invariant
  #   (Field::Base#validate_parent_scope_invariant from plan 03) prevents
  #   the row from ever existing in the DB. This is a wholly new validator
  #   introduced by Phase 1 — there is no two-level scope ancestor to extend.
  #
  # Scenario B — Orphan-parent rejection at Section level: symmetric guard
  #   inline-duplicated on Section per the CONTEXT.md "do NOT extract
  #   Scopable" decision. Same rationale as A.
  #
  # Scenario C — Cross-(scope, parent_scope) Value rejection: a Value
  #   attached to a Field whose parent_scope doesn't match the entity's
  #   typed_eav_parent_scope is invalid. The Value-side guard
  #   (Value#validate_field_scope_matches_entity from plan 04) extends the
  #   single-axis check to the second axis. Lives at a different layer than
  #   round_2 (definition-time leak) and round_3 (definition-time collision).
  RSpec.describe "Round-4 review: parent_scope cross-cutting", :scoping do
    before do
      TypedEAV.config.scope_resolver = nil
      TypedEAV.config.require_scope = true
    end

    after { TypedEAV.config.reset! }

    describe "Scenario A: orphan-parent rejection at Field level" do
      it "rejects parent_scope.present? && scope.blank? at validation" do
        f = build(:integer_field, name: "x", entity_type: "Project", scope: nil, parent_scope: "w1")
        expect(f).not_to be_valid
        expect(f.errors[:parent_scope]).to be_present
      end

      it "accepts (scope set, parent_scope set) — the canonical full-triple shape" do
        f = build(:integer_field, name: "y", entity_type: "Project", scope: "t1", parent_scope: "w1")
        expect(f).to be_valid
      end
    end

    describe "Scenario B: orphan-parent rejection at Section level (symmetric guard)" do
      it "rejects via Section as well (inline-duplicated invariant per CONTEXT.md)" do
        s = TypedEAV::Section.new(name: "S", code: "x", entity_type: "Project", scope: nil, parent_scope: "w1")
        expect(s).not_to be_valid
        expect(s.errors[:parent_scope]).to be_present
      end

      it "accepts (scope set, parent_scope set) on Section — symmetric to Field" do
        s = TypedEAV::Section.new(name: "S2", code: "y", entity_type: "Project", scope: "t1", parent_scope: "w1")
        expect(s).to be_valid
      end
    end

    describe "Scenario C: cross-(scope, parent_scope) Value rejection" do
      let!(:full_triple_field) { create(:integer_field, name: "x", entity_type: "Project", scope: "t1", parent_scope: "w1") }
      let!(:project_w1) { create(:project, tenant_id: "t1", workspace_id: "w1") }
      let!(:project_w2) { create(:project, tenant_id: "t1", workspace_id: "w2") }

      it "rejects a Value bound to a project in a different workspace" do
        v = TypedEAV::Value.new(entity: project_w2, field: full_triple_field, value: 1)
        expect(v).not_to be_valid
      end

      it "accepts a Value bound to a project in the matching workspace" do
        v = TypedEAV::Value.new(entity: project_w1, field: full_triple_field, value: 1)
        expect(v).to be_valid
      end

      it "rejects a Value bound to an entity host without parent_scope_method (Contact -> full-triple field)" do
        contact = create(:contact, tenant_id: "t1")
        v = TypedEAV::Value.new(entity: contact, field: full_triple_field, value: 1)
        expect(v).not_to be_valid
      end
    end
  end

Aim for ~80-100 lines (smaller than the original 150-line draft because the unscoped/multimap and three-way-precedence content moved to round_2/round_3 extensions).
  </action>
  <verify>
`bin/rspec spec/regressions/review_round_4_parent_scope_spec.rb` — all examples pass. Inspect the file structure: three describe blocks (Scenario A/B/C), header comment explicitly states this file owns ONLY cross-cutting scenarios and points readers to round_2/round_3 for leak/collision coverage. `git diff --stat spec/regressions/review_round_4_parent_scope_spec.rb` shows the file was created with the expected size.
  </verify>
  <done>
review_round_4_parent_scope_spec.rb exists with cross-cutting scenarios only, follows the round_2/round_3 file shape, and is green.
  </done>
</task>
<task type="auto">
  <name>Run full suite and commit</name>
  <files>
    spec/lib/typed_eav/scoping_spec.rb
    spec/regressions/review_round_2_scope_leak_spec.rb
    spec/regressions/review_round_3_collision_spec.rb
    spec/regressions/review_round_4_parent_scope_spec.rb
    spec/models/typed_eav/value_spec.rb
    spec/models/typed_eav/field_spec.rb
    spec/factories/typed_eav.rb
  </files>
  <action>
From spec/dummy: `bin/rails db:drop db:create db:migrate` for a clean slate, then `bin/rspec` for the full suite. The expected outcome is 0 failures, 0 errors. Pre-existing scope-related tests now pass because their assertion shape matches the tuple resolver, and the new parent_scope coverage exercises the production code wired up in plan 05.

If any spec FAILS that isn't covered by the changes in this plan, investigate. Most likely cause: a misalignment between the tuple resolver (plan 02) and a consumer not updated in plan 05. Pause and document — do NOT modify production code from this spec plan. Surface the discovery for a follow-up.

Pre-existing failures unrelated to Phase 1 (if any) should be flagged as "pre-existing issues" per project policy and reported back rather than fixed here.

Commit message: `test(scope): cover parent_scope axis across resolver, query path, regressions, and unit specs`. Single commit. Seven files modified.

Do NOT bump version, push, or update CHANGELOG.
  </action>
  <verify>
`bin/rspec` from spec/dummy reports the expected counts. Capture the run summary in the SUMMARY.md (count of examples, count of new examples added by this plan).

`git log --oneline -1` shows the commit. `git diff HEAD~1 --stat` shows exactly the seven files listed (scoping_spec, round_2, round_3, round_4, value_spec, field_spec, factories).
  </verify>
  <done>
Full RSpec suite green; single atomic commit; no production-code files touched in this commit.
  </done>
</task>
</tasks>
<verification>
1. `bin/rspec spec/lib/typed_eav/scoping_spec.rb` — green, asserts tuple shape, covers tuple-form with_scope, parent_scope kwarg, ArgumentError on bare-scalar resolver.
2. `bin/rspec spec/regressions/review_round_2_scope_leak_spec.rb` — green, includes the new "Project (scope_method: only ...)" describe block plus parent_scope-axis it blocks added to existing Product/Contact blocks. `git diff --stat spec/regressions/review_round_2_scope_leak_spec.rb` shows file modified (not just touched).
3. `bin/rspec spec/regressions/review_round_3_collision_spec.rb` — green, includes "Bug 1 mirror" (multimap collapse across (scope, parent_scope)) and "Bug 2 mirror" (three-way precedence) describe blocks. `git diff --stat spec/regressions/review_round_3_collision_spec.rb` shows file modified.
4. `bin/rspec spec/regressions/review_round_4_parent_scope_spec.rb` — green, covers ONLY cross-cutting scenarios (Scenarios A/B/C: orphan-parent rejection at Field, orphan-parent rejection at Section, cross-axis Value rejection).
5. `bin/rspec spec/models/typed_eav/value_spec.rb spec/models/typed_eav/field_spec.rb` — green, includes parent_scope axis assertions.
6. `bin/rspec` (full suite) — 0 failures, 0 errors.
7. `git diff main -- spec/` shows new and updated files.
8. `:project` factory exists in spec/factories/typed_eav.rb.
</verification>
<success_criteria>
- Full test suite is green on a clean db reset.
- Existing scope assertions match the new tuple contract.
- review_round_2 owns parent_scope leak coverage as a first-class regression alongside its existing scope coverage.
- review_round_3 owns three-way precedence and parent_scope multimap collapse alongside its existing two-way precedence.
- review_round_4 file follows project conventions, contains ONLY cross-cutting scenarios (orphan-parent + cross-axis Value rejection), and is self-explanatory.
- Single atomic commit; no production-code files touched.
- The SUMMARY.md notes the count of new examples added (for the milestone-level test-count delta), broken out per file.
</success_criteria>
<output>
01-06-SUMMARY.md
</output>

## Plan amendment (R01) — single-commit consolidation + corrected task 3 assertion

**DEVIATION-01-06-A (single-commit consolidation)**: The QA gate flagged that tasks 1-4 shipped as a single coordinated edit rather than four sequential commits. This IS the prescribed execution model: task 5 explicitly directs "Single commit. Seven files modified." Tasks 1-4 spread across seven spec files (`scoping_spec.rb`, `review_round_2_scope_leak_spec.rb`, `review_round_3_collision_spec.rb`, the new `review_round_4_parent_scope_spec.rb`, `value_spec.rb`, `field_spec.rb`, `factories/typed_eav.rb`), but each task's done criteria were independently verified via per-file rspec runs before staging (task 1: 44 ex 0 fail on `scoping_spec`; task 2: 187 ex 0 fail on `value_spec` + `field_spec`; task 3: 33 ex 0 fail across `round_2`/`round_3`; task 4: 14 ex 0 fail on `round_4`). Shipped as a single atomic commit (commit `e5e78a4`). Task numbering is organizational decomposition, not commit boundaries.

**DEVIATION-01-06-B (corrected task 3 assertion — plan-vs-production mismatch)**: Task 3 (the `typed_eav_definitions parent_scope kwarg` describe block in `scoping_spec.rb`) prescribed the assertion `expect(fields).to contain_exactly(project_scope_only, project_global)` for `Project.typed_eav_definitions(parent_scope: nil)` inside a `with_scope(["tenant_a", "w1"]) { ... }` block. This contradicts production semantics. The actual `resolve_scope` implementation at `lib/typed_eav/has_typed_eav.rb:340` gates ambient resolution off whenever EITHER kwarg is passed explicitly — `explicit_given = !explicit_scope.equal?(UNSET_SCOPE) || !explicit_parent_scope.equal?(UNSET_SCOPE)`. Passing `parent_scope: nil` is an explicit pass (not `UNSET_SCOPE`), so the resolver collapses to `(scope: nil, parent_scope: nil)` and `for_entity` returns globals only — `project_scope_only` is filtered out by the `scope IN (NULL)` clause. Dev caught the planning error during execution and rewrote the test to assert actual behavior (`contain_exactly(project_global)`) with an explanatory inline comment, parallel to the existing `scope: nil` test in `spec/lib/typed_eav/scoping_spec.rb`. No production code was touched. The corrected assertion is the canonical plan-task-3 expectation; the original assertion was a planning error that did not consult the production semantics. Future planning should consult `resolve_scope`'s any-explicit-disables-ambient rule (the `explicit_given` branch at `has_typed_eav.rb:340`) before prescribing assertions for `Project.typed_eav_definitions(...)` calls inside active `with_scope` blocks. When in doubt, see the inline comment alongside the corrected test in `scoping_spec.rb`.

---
phase: 6
plan: 01
title: version_group_id migration + subscriber injection point
type: execute
wave: 1
depends_on: []
cross_phase_deps: ["phase-4"]
autonomous: true
effort_override: balanced
skills_used: [database-migrations, tdd-cycle, rails-architecture]
files_modified:
  - db/migrate/20260506000001_add_version_group_id_to_typed_eav_value_versions.rb
  - lib/typed_eav/versioning/subscriber.rb
  - spec/models/typed_eav/value_version_spec.rb
  - spec/lib/typed_eav/versioning/subscriber_spec.rb
forbidden_commands: []
must_haves:
  truths:
    - "New migration db/migrate/20260506000001_add_version_group_id_to_typed_eav_value_versions.rb adds a nullable `version_group_id :uuid` column to `typed_eav_value_versions` and a concurrent index `idx_te_vvs_group` on `(version_group_id)`. Migration uses `disable_ddl_transaction!` and explicit `up` / `down` methods (no `change`) per the canonical production-safety template at `db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb:1–8` (RESEARCH §Versioning integration Anchor 6)."
    - "Column type is Postgres `:uuid` (not `:string`). No `null: false`, no default — non-bulk writes leave it NULL (backward compatible per CONTEXT §version_grouping default). Resolved per RESEARCH §Open-Question Resolutions §version_group_id type."
    - "Index name is exactly `idx_te_vvs_group` (matches the existing `idx_te_vvs_*` family on this table — see CONVENTIONS.md §Naming and `db/migrate/20260505000000_create_typed_eav_value_versions.rb:33–94`). `add_index` uses `algorithm: :concurrently, if_not_exists: true`; `remove_index` (in `down`) uses `if_exists: true, algorithm: :concurrently`."
    - "Migration is reversible: running `db:migrate` then `db:rollback` then `db:migrate` succeeds with the column and index in place at the end."
    - "`lib/typed_eav/versioning/subscriber.rb` `ValueVersion.create!(...)` call at line 110–121 reads `context[:version_group_id]` and passes it to `ValueVersion.create!` as `version_group_id: context[:version_group_id]`. When the key is absent the value is nil — backward-compatible. The 3-arg subscriber signature `(value, change_type, context)` is unchanged (RESEARCH §Versioning integration Anchor 2)."
    - "`TypedEAV::ValueVersion` AR model needs NO change (additive column is auto-discovered from schema; verified against CONTEXT §Files Likely to Change §Modify)."
    - "`Config.versioning` runtime gate: when `version_group_id` is present in `context` BUT `Config.versioning = false`, the subscriber is not registered at engine boot, so the value never reaches `ValueVersion.create!`. No defensive code needed in the subscriber for the disabled-versioning path — Plan 03 enforces an `ArgumentError` at the bulk-call site (CONTEXT §version_grouping default & disabled-versioning behavior)."
    - "Spec `spec/models/typed_eav/value_version_spec.rb` is extended with one test asserting that `ValueVersion.new.respond_to?(:version_group_id)` is true and that a version row created with `version_group_id: SecureRandom.uuid` round-trips on reload. No new spec file required."
    - "Spec `spec/lib/typed_eav/versioning/subscriber_spec.rb` is extended with one test asserting that wrapping a Value mutation in `TypedEAV.with_context(version_group_id: uuid) { value.update!(...) }` produces a version row whose `version_group_id` equals the supplied uuid. Test uses `:event_callbacks` and `:real_commits` metadata flags (RESEARCH §Test Infrastructure §Spec metadata flags)."
    - "No engine-boot change: subscriber registration in `lib/typed_eav/versioning.rb:85–92` is unchanged. The runtime read of `context[:version_group_id]` works whether or not a caller passes the key."
    - "frozen_string_literal: true magic comment is NOT required on the migration file (CONVENTIONS.md §Style & lint excludes `db/migrate/**/*`). It IS required on any new spec / lib file change."
    - "Foundational principle preserved: `version_group_id` is a runtime-resolved correlation tag passed through context — no hardcoded attribute references. The subscriber receives `value`, `change_type`, `context` as parameters and reads `context[:version_group_id]` by key, never inferring it from a record attribute."
  artifacts:
    - path: "db/migrate/20260506000001_add_version_group_id_to_typed_eav_value_versions.rb"
      provides: "Additive uuid column + concurrent index on typed_eav_value_versions"
      contains: "add_column :typed_eav_value_versions, :version_group_id, :uuid"
    - path: "lib/typed_eav/versioning/subscriber.rb"
      provides: "Subscriber reads context[:version_group_id] and passes to ValueVersion.create!"
      contains: "version_group_id: context[:version_group_id]"
    - path: "spec/models/typed_eav/value_version_spec.rb"
      provides: "Round-trip spec for version_group_id column"
      contains: "version_group_id"
    - path: "spec/lib/typed_eav/versioning/subscriber_spec.rb"
      provides: "Subscriber-side spec asserting context[:version_group_id] flows to the version row"
      contains: "with_context(version_group_id:"
  key_links:
    - from: "db/migrate/20260506000001_add_version_group_id_to_typed_eav_value_versions.rb"
      to: "db/migrate/20260505000000_create_typed_eav_value_versions.rb"
      via: "additive ALTER TABLE — Phase 4's table is the parent migration; this column joins the existing `idx_te_vvs_*` index family (CONVENTIONS.md §Naming line 117)"
    - from: "lib/typed_eav/versioning/subscriber.rb"
      to: "lib/typed_eav.rb (TypedEAV.with_context / current_context)"
      via: "subscriber reads `context` argument the dispatcher already supplies — no new wiring; consistent with the existing `context: context.to_h` line at subscriber.rb:118"
    - from: "spec/lib/typed_eav/versioning/subscriber_spec.rb"
      to: "spec/spec_helper.rb :event_callbacks + :real_commits hooks"
      via: "version_group_id specs MUST use both flags — context flows through after_commit on the OUTER transaction commit (RESEARCH §Risks §Savepoint-in-after_commit interaction)"
---
<objective>
Land the additive `version_group_id` column on `typed_eav_value_versions` and wire the Phase 4 versioning subscriber to read `context[:version_group_id]` and pass it to `ValueVersion.create!`. After this plan: a column exists; a concurrent index exists; the subscriber forwards the correlation tag if a caller wraps the write in `TypedEAV.with_context(version_group_id: uuid) { ... }`. NO bulk write API yet — that lands in Plan 03. NO callers — non-bulk writes leave the column NULL. This is the migration + subscriber prep work that unblocks the bulk write integration.

Why isolated: this plan touches `db/migrate/` (a new file) and `lib/typed_eav/versioning/subscriber.rb` (one-line edit at the `ValueVersion.create!` call). Plan 03 (bulk write) needs this column to exist BEFORE it can inject `version_group_id` via `with_context`. Splitting the migration into its own plan/wave avoids a same-wave conflict where Plan 03 would try to use a column that the migration plan hasn't created yet — even within a single Dev session, the migration must run before the bulk-write integration test asserts a populated `version_group_id` on a version row.
</objective>
<context>
@.vbw-planning/phases/06-bulk-operations/06-CONTEXT.md
@.vbw-planning/phases/06-bulk-operations/06-RESEARCH.md
@.vbw-planning/codebase/PATTERNS.md
@.vbw-planning/codebase/CONVENTIONS.md
@/Users/darrindemchuk/.claude/skills/database-migrations/SKILL.md
@/Users/darrindemchuk/.claude/skills/tdd-cycle/SKILL.md
@/Users/darrindemchuk/.claude/skills/rails-architecture/SKILL.md

Locked decisions binding on this plan (from 06-CONTEXT.md):

- **Mechanism:** Add a nullable, indexed `version_group_id uuid` column to `typed_eav_value_versions`. Additive migration on top of Phase 4's table. Uses `disable_ddl_transaction!` + `algorithm: :concurrently` per the production-safety pattern (CONTEXT §version_grouping default & disabled-versioning behavior).
- **Default behavior:** Bulk operations populate `version_group_id`; non-bulk writes leave it NULL (backward-compatible).
- **UUID type (Plan-time resolution per RESEARCH §Open-Question Resolutions):** Postgres `:uuid` column type. Postgres-only commitment is binding — see ROADMAP §Cross-cutting requirements. `:uuid` stores 16 bytes vs 36 for a string and is the idiomatic choice.

Anchors confirmed in research:
- Migration template at `db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb:1–8` (RESEARCH §Migration Template Anchor).
- Phase 4 table created at `db/migrate/20260505000000_create_typed_eav_value_versions.rb:33–94` — three existing `idx_te_vvs_*` indexes; the new one joins this family.
- Subscriber `ValueVersion.create!` call at `lib/typed_eav/versioning/subscriber.rb:110–121` — exact insertion site for `version_group_id: context[:version_group_id]`.
- `Config.versioning` runtime check is in Plan 03's bulk-call path, NOT in the subscriber (RESEARCH §Versioning integration Anchor 3).

⚠ Discrepancy awareness:
- The `idx_te_vvs_*` index name family fits Postgres' 63-byte limit (CONVENTIONS.md line 117). New index name `idx_te_vvs_group` is well within budget.
- `disable_ddl_transaction!` is REQUIRED because `algorithm: :concurrently` cannot run inside a transaction (RESEARCH §Migration Template Anchor).
- The subscriber currently passes `context: context.to_h` to `ValueVersion.create!` (already a column) — that's the existing context jsonb column, distinct from `version_group_id`. Both stay populated independently. The subscriber edit is additive — the existing `context: context.to_h` line is preserved.
</context>
<tasks>
<task id="T1" effort="medium">
<title>Add migration: version_group_id column + concurrent index on typed_eav_value_versions</title>
<files_to_touch>
  - db/migrate/20260506000001_add_version_group_id_to_typed_eav_value_versions.rb (new)
</files_to_touch>
<acceptance>
  - File exists at the exact path with timestamp `20260506000001` (sequential after Phase 4's `20260505000000`).
  - Class name `AddVersionGroupIdToTypedEavValueVersions` (Rails-conventional camelization of the filename).
  - `disable_ddl_transaction!` declared at class level.
  - Explicit `def up` and `def down` methods (NOT `change`).
  - `up`: `add_column :typed_eav_value_versions, :version_group_id, :uuid` (no `null: false`, no default), then `add_index :typed_eav_value_versions, :version_group_id, name: "idx_te_vvs_group", algorithm: :concurrently, if_not_exists: true`.
  - `down`: `remove_index :typed_eav_value_versions, name: "idx_te_vvs_group", if_exists: true, algorithm: :concurrently`, then `remove_column :typed_eav_value_versions, :version_group_id`.
  - Inline comment at top explaining: (a) why uuid not bigint (correlation tag without a shared sequence; Postgres-only commitment per ROADMAP), (b) why nullable (non-bulk writes leave NULL), (c) why concurrent index (production-safety pattern).
  - `bin/rails db:migrate` runs cleanly against the test database; `bin/rails db:rollback` reverses cleanly; `bin/rails db:migrate` re-applies cleanly.
  - No `frozen_string_literal: true` magic comment (CONVENTIONS.md excludes `db/migrate/**/*`).
</acceptance>
<notes>
Mirror the structure of `db/migrate/20260430000000_add_parent_scope_to_typed_eav_partitions.rb` exactly. The only meaningful differences are: target table, column name, column type (`:uuid` vs the prior plan's `:string`), and index name. Do NOT add a `null: false` constraint — every existing version row was created without this column and has implicit NULL; adding `null: false` would require a backfill that is explicitly out of scope per CONTEXT §version_group_id default.
</notes>
</task>

<task id="T2" effort="low">
<title>Wire subscriber to read context[:version_group_id] and forward to ValueVersion.create!</title>
<files_to_touch>
  - lib/typed_eav/versioning/subscriber.rb (modify line ~110–121)
</files_to_touch>
<acceptance>
  - The `ValueVersion.create!(...)` call inside `Subscriber.persist!` (or whichever method wraps the create — see subscriber.rb:110) gains one new keyword argument: `version_group_id: context[:version_group_id]`.
  - Argument is positioned next to the existing `context: context.to_h` line (line 118) so the two related context-derived arguments are visually grouped.
  - When `context[:version_group_id]` is absent (the common path: non-bulk writes), the value is `nil` and `ValueVersion.create!` records nil — column is nullable, no validation fires.
  - One inline comment above the new line explaining: "Bulk operations (Plan 06-03) inject this UUID via `TypedEAV.with_context(version_group_id: uuid) { ... }`. Non-bulk writes omit the key; column stays NULL. Backward-compatible: unchanged subscribers and unchanged callers continue to work."
  - The 3-arg signature of the public subscriber method `(value, change_type, context)` is UNCHANGED.
  - No engine-boot change: registration in `lib/typed_eav/versioning.rb:85–92` continues to read `Config.versioning` once at boot.
  - `bin/rspec spec/lib/typed_eav/versioning/` passes (existing specs continue to pass — context behavior is additive).
</acceptance>
<notes>
DO NOT change the subscriber's method signature. DO NOT add validation in the subscriber for the `Config.versioning = false` + `version_group_id present` case — that's Plan 03's responsibility at the call site. The subscriber's contract is "if I'm registered, write the version row with whatever context I receive." Plan 03 enforces the disabled-versioning ArgumentError before reaching the subscriber.
</notes>
</task>

<task id="T3" effort="low">
<title>Spec: ValueVersion column round-trip + subscriber spec asserting context flow</title>
<files_to_touch>
  - spec/models/typed_eav/value_version_spec.rb (extend)
  - spec/lib/typed_eav/versioning/subscriber_spec.rb (extend)
</files_to_touch>
<acceptance>
  - `spec/models/typed_eav/value_version_spec.rb`: one new `describe "#version_group_id" do ... end` block with two examples: (1) responds_to :version_group_id, (2) `create!(version_group_id: SecureRandom.uuid)` round-trips through reload.
  - `spec/lib/typed_eav/versioning/subscriber_spec.rb`: one new example wrapping a Value `update!` inside `TypedEAV.with_context(version_group_id: uuid) { ... }` and asserting that `Value.last.versions.last.version_group_id == uuid`. Spec metadata: `:event_callbacks, :real_commits` (RESEARCH §Test Infrastructure §Spec metadata flags). Use `Config.versioning = true` for this example only — restored by the `:event_callbacks` snapshot/restore.
  - One additional negative example: a Value mutation WITHOUT a wrapping `with_context(version_group_id: ...)` block produces a version row with `version_group_id == nil` (backward-compat assertion).
  - All new specs use `frozen_string_literal: true` magic comment (CONVENTIONS.md §Style & lint).
  - `bin/rspec spec/models/typed_eav/value_version_spec.rb spec/lib/typed_eav/versioning/subscriber_spec.rb` runs green.
  - Bullet (if enabled in test env) does NOT flag N+1 — bulk read still won't be exercised here (that's Plan 02).
</acceptance>
<notes>
The `:real_commits` flag is mandatory for the subscriber assertion because `after_commit` only fires on outer-transaction commit; transactional fixtures rolled back before commit would suppress the version row entirely. Cleanup order in `:real_commits` already handles `ValueVersion.delete_all` → `Value.delete_all` (spec_helper.rb:165–173). Do NOT add cleanup; it already exists.
</notes>
</task>
</tasks>
<verification>
- `bin/rails db:migrate` runs cleanly against `spec/dummy` (the test/dev database).
- `bin/rails db:rollback STEP=1` reverses cleanly (column and index removed).
- `bin/rails db:migrate` re-applies cleanly (schema returns to with-column state).
- `bin/rspec spec/models/typed_eav/value_version_spec.rb spec/lib/typed_eav/versioning/` runs green.
- `bin/rubocop db/migrate/20260506000001_add_version_group_id_to_typed_eav_value_versions.rb lib/typed_eav/versioning/subscriber.rb spec/models/typed_eav/value_version_spec.rb spec/lib/typed_eav/versioning/subscriber_spec.rb` reports no offenses.
- Schema integrity check: a foreign-key audit script (or `bin/rails db:schema:dump` + grep) confirms the `idx_te_vvs_group` index exists on `typed_eav_value_versions(version_group_id)`.
- No N+1 regression: existing Phase 4 specs continue to pass without new query-count regressions.
</verification>
<success>
- Migration ships, runs, rolls back cleanly. Column is `:uuid`, nullable, indexed concurrently with `idx_te_vvs_group`.
- Subscriber forwards `context[:version_group_id]` to the version row when present; preserves NULL when absent.
- ValueVersion model surfaces the column without code change (auto-discovered).
- Round-trip + subscriber-flow specs prove the wiring end-to-end.
- Plan 03 (bulk write) can now safely depend on this column existing and on the subscriber consuming it via `with_context`.
- ROADMAP §Phase 6 success criterion "version_grouping: :per_record | :per_field when versioning is enabled" is unblocked at the schema layer (full API lands in Plan 03).
</success>

## Out of scope for this plan

- The `bulk_set_typed_eav_values` API itself — Plan 03.
- The `version_grouping:` keyword argument and the `:per_record` / `:per_field` UUID-generation logic — Plan 03.
- The `ArgumentError` raise when `version_grouping:` is passed but `Config.versioning = false` — Plan 03.
- Backfilling `version_group_id` on existing version rows — explicitly NOT a goal (column is nullable; existing rows stay NULL).
- A `ValueVersion.where(version_group_id: x).order(...)` query API helper — out of scope for v0.6.0; callers use AR directly.
- Multi-cell field type (Currency) coverage — irrelevant to this layer; the version_group_id is per-version-row regardless of field type.

## Plan Amendments

### DEV-01: Migration timestamp bump (20260506000000 → 20260506000001)

**Classification:** plan-amendment (pre-amended in-place by the orchestrator before execution).

**Original plan-time intent:** Filename and timestamp `db/migrate/20260506000000_add_version_group_id_to_typed_eav_value_versions.rb`. The plan author selected `20260506000000` as the next sequential timestamp after Phase 04's `db/migrate/20260505000000_create_typed_eav_value_versions.rb`.

**Reality at execution:** The dummy app already shipped `spec/dummy/db/migrate/20260506000000_create_active_storage_tables.active_storage.rb` (the Phase 05 dummy-app Active Storage migration). Two migrations cannot share a timestamp — Rails' `MigrationContext` raises `Multiple migrations have the version number 20260506000000` and refuses to run. The orchestrator detected the collision and pre-amended every reference in this plan from `20260506000000` to `20260506000001` BEFORE the agent ran. The Dev agent then implemented against the already-amended plan.

**Functional impact:** None. The migration class name `AddVersionGroupIdToTypedEAVValueVersions` is unchanged (Rails-conventional camelization of the filename body, not of the timestamp). The schema change (additive nullable `version_group_id :uuid` column + concurrent `idx_te_vvs_group` index) is identical. Migrate / rollback / re-migrate cycle verified clean against `spec/dummy` (06-VERIFICATION.md MH-01 PASS).

**Why this amendment is the source of truth:** The bump is a sequencing fix forced by the dummy app's existing migrations; it has no behavioural or schema impact and is strictly safer than the original timestamp (no collision, no manual operator intervention, deterministic re-runs). Future remediation rounds and downstream phases should reference `20260506000001` exclusively.

**Cross-references:**
- 06-01-SUMMARY.md `deviations` field DEVN-02 carries the same rationale.
- 06-VERIFICATION.md row DEV-01 (FAIL) is the QA evidence prompting this written record.
- The implementation commit is `f21f607` ("feat(versioning): add version_group_id uuid column to typed_eav_value_versions").
